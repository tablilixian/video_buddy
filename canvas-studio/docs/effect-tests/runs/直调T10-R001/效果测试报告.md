# 效果测试报告（直调模式）

> 本目录是 **T10 · Ref2VA 直出 vs 关键帧 I2VA 对照** 的直调执行存档（不走画布创作流程，与 T9 直出模式同规格）。用例定义：`skills-local/effect-test-runner/references/fixtures.md` T10 节；长期用例文档：`docs/effect-tests/视频效果验证测试用例与轮次记录.md` §T10。

## R002 · T10（2026-09-16 · CV-193 批次 · 工作区未提交）

- **执行方式**：画布外直调 Drama Backend（curl 逐端点），非画布链路——只测「接口 × 提示词 × 固定素材」的质量问题，不测 skill 服从率
- **判定**：结构判定**通过**（A/B 全部自动项过）；路线优劣**待人工并排评**（本用例核心产出）
- **关键参数**：
  - A 路线（Ref2VA 直出）：`image2videoref2va`，filenames=3 张固定图（两张四视图锚点 + 竹林场景图），megapixels=1.0，duration=8 → 产物 `MiniMax_H3_ref2va_00010_.mp4`（1376×768 · 8.00s · 24fps · 含音轨）
  - B 路线（关键帧基线）：`image2image`（3 参考，Krea2 Edit）出姿态关键帧 `krea2_edit_ref_00019_.png` → `image2videofl2va` 单图首帧，megapixels=1.0，duration=8 → 产物 `MiniMax_H3_fl2va_00011_.mp4`（1376×768 · 8.00s · 24fps · 含音轨）
- **prompt 全文**：逐字存档于本目录 `payload-routeA-ref2va.json` / `payload-routeB-keyframe.json` / `payload-routeB-fl2va.json`（与 fixtures.md T10 金标准一致，无改写）
- **自动评**：
  - A 路线：路由 `image2videoref2va` ✅（3 张参考）；六段式 6/6，`summary` 以 `[reference generation]` 开头 ✅；`retention_analysis` 三条均 `fully_preserved` ✅；无 `<Video N>` / `<Audio N>` ✅
  - B 路线：关键帧 `image2image` 成功 ✅（关键帧质量高：双角色身份、构图、雾竹林氛围全部到位，见 `routeB-keyframe.png`）；路由 `image2videofl2va` ✅（单图首帧）；首行对齐指令 + 三字段 ✅
  - image2vl 抽帧比对（阈值 70%，中帧 n=96）：
    - B 帧 × 灰袍剑客四视图锚点：**交集 100%**（长袍/马尾/黑/棱角脸全中）
    - A 帧 × 风衣男四视图锚点：**交集 25%**——以归一化伪差为主：同一件深蓝风衣在锚点被标「风衣(trench)」、在帧被标「深蓝(navy)」；人工目检 A 帧服装/发型与锚点一致
    - ⚠️ 方法学限制：单人物 prompt 对双人镜头只描述最显著的一人（A 帧描述了风衣男、B 帧描述了灰袍剑客），跨角色交集不可比
    - 场景：A 帧（土路/雾/林 29%）与 B 帧（土路/竹/雾 43%）均命中核心场景元素；B 帧金色光束更贴锚点图的 golden light
- **人工目检发现（单帧观察，待并排评确认）**：
  - **B 帧（关键帧路线）风衣男出现马尾漂移**（锚点为短发），疑似被灰袍角色「传染」；A 帧（直出路线）风衣男短发贴锚点
  - B 帧 4s 处风衣男也持械（A/B prompt 均只给剑客配刀）——两条路线都有程度不一的道具外溢
- **待人工评**：C 表现力 __ / D 节奏 __；A/B 并排四维（锚点相像度 / 动作自然度 / 构图符合度 / 画质）：__
  - 产物：`routeA-video.mp4` / `routeB-video.mp4` / 中帧 `routeA-frame.png` / `routeB-frame.png` / 关键帧 `routeB-keyframe.png`
- **备注（重要运营发现）**：
  1. **双路线均超管线视频超时**：服务端实测 A **645.0s** / B **751.4s**（@768p/8s），均超 `DRAMA_TIMEOUT_MS.video`（600s）——按当前后端速度，默认 768p 档 8s 视频无论走哪条路线都会被管线客户端超时掐断。历史上（2026-09-15 CV-187 探针）同规格 fl2va 仅 130.8s；当前显著变慢。短期缓解：草稿/试拍走 480p（管线实测约 6 倍提速），或调高管线视频超时
  2. **A 路线首次 600s 无响应（HTTP 000）**：客户端断开后服务端任务不回收（api-usage 已知行为），推测该任务在服务端继续排队完成或被新任务覆盖；期间后端 health 一度失联约 45s 后自愈（与 CV-187 P0-d「长任务下整体不可达」现象一致）
  3. 素材派生链：`character-anchor-4view.png` / `character2-anchor-4view.png` 由各自定妆照经 `image2character`（krea2_quadview）派生；`bamboo-scene.png` 由 `txt2image`（Krea2 Turbo）固定 prompt 生成——三张均已入库 `skills-local/effect-test-runner/assets/`，跨轮可比
- **轮次记录表待粘贴行**：`R002 | 2026-09-16 | 工作区未提交（CV-193 批次） | 新 | T10 | 通过（结构） | 双路线均超管线600s超时（A 645s/B 751s@768p）；B帧灰袍角色锚点交集100%；B帧风衣男疑似马尾漂移、A帧贴锚点——路线优劣待人工并排评 | docs/effect-tests/runs/直调T10-R001`
