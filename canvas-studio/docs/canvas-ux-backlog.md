# 画布客户端 UX 待办清单（Canvas UX Backlog）

> 📌 **状态以 [STATUS.md](./STATUS.md) 为准，本文件只保留技术细节**（根因、方案、涉及文件、逐次变更记录）。
> 更新流程见 [DEV-WORKFLOW.md](./DEV-WORKFLOW.md#四收尾必更文档强制步骤)。
> 文档索引见 [README.md](./README.md)。
>
> 🗄️ **已完成的条目已不在这里** —— 35 条「已完成」（代码落地 **且** 桌面验收通过）已于 2026-09-03 抽出封存到
> [archive-cv-completed.md](./archive-cv-completed.md)。该存档**只读**：不要在其中改状态、加条目、记新问题，
> 也不要从那里找待办。要追溯「当年为什么这么做」才去看它。

> 来源：2026-08-28 对 `src/client/` 全部画布组件的代码级审查（含 StudioFrame / project-store / question-capture）。
> 用途：作为画布逐步优化的追踪清单。每处理完一项，更新 STATUS.md 的状态并在本文件文末「变更记录」追加一行。
>
> 状态词汇见 [STATUS.md §0](./STATUS.md#0-怎么用这份表)（`待处理` / `已修复·待验收` / `已完成` / `待拍板` / `暂缓` 等）。
> 本表状态列已于 2026-09-03 按 STATUS.md 全量同步一次；**两者冲突时以 STATUS.md 为准**。
> 优先级：`P0`（bug / 功能断裂）> `P1`（核心工作流缺口）> `P2`（体验优化）> `P3`（低优先级 / 远期）。

## P0 — 真 bug / 功能断裂

| ID | 状态 | 优先级 | 问题 | 涉及文件 | 改进意见 |
| --- | --- | --- | --- | --- | --- |
| CV-036 | 待拍板 | P1 | 项目没有「已完成」标记：用户无法区分进行中与已完成的项目（2026-08-28 用户提出，设计方案待拍板，见下方讨论记录） | `projects.ts`（project entry）、`ProjectList.tsx`、`StudioFrame.tsx` | 建议方案 A（手动标记 + 成片信号辅助）：① project entry 增 `completedAt?: number`；② 项目列表行 + 画布工具条均可切换「标记完成」，列表行显示 ✅ 徽章、可选沉底排序；③ 画布出现成功的成片节点（compose/video-composite 产物）时 toast 提示「标记为已完成？」——成片是信号不是自动状态，手动确认才落标记；可随时取消。备选方案 B（全自动：有成片即已完成）——不推荐，迭代返工的项目会被误标 |
| CV-039 | 待处理（部分） | P1 | skill 的 H3 提示词规范是官方版的粗糙子集，声音设计能力完全没启用：H3 原生联合生成音视频（32kHz 立体声），对白（`(S1)` 说话人 ID + `<d>[语言]台词</d>`）、BGM（`non_diegetic_music` 字段）、环境音效（`overall_soundscape` 字段）、画面内文字（引号标注）都由提示词驱动，但 creation-spec 只教了画面描述。官方规范来源：MiniMax-H3 开源仓库 `skills/h3-prompt-writing/references/`（base-en.txt / ref-en.txt） | `skills/creation-spec.ts`（H3 提示词规范 + 镜头词汇）、必要时 `generate.ts`（提示词组装） | 升级为官方三字段结构：`integrated_multimodal_description`（画面+对白+画内声，含 `[Shot N] At MM:SS.mmm` 切镜时间戳）+ `overall_soundscape`（1–4 句环境音总结）+ `non_diegetic_music`（1–3 句画外配乐，禁抽象情绪词）；运镜三维表达（类型+幅度+速度，自然英语句式）；对白说话人 ID 与口型同步规则；画面文字引号规范；Ref2VA 模式补六段结构（`<Subject N>`/`<Picture N>`/`<Video N>`/`<Audio N>` 标签体系 + retention_analysis）。write_script 的对白/BGM/音效说明直接映射进三字段——文案节点成为声音设计的叙事锚点 |
| CV-040 | 待处理 | P1 | 多段拼接的成片音轨断裂：每段视频独立生成时音轨互不相干，拼起来 BGM/节奏对不上。官方解法（H3 开源仓库 MV skill）：锁一条全局 Master Audio（完整 BGM/歌曲）为唯一基准 → 分镜各自生成（提示词的 BGM 字段保持全局一致）→ 剪辑装配时全部对齐 Master Audio 时间线。注意：仍需一步「对齐+拼接」音频装配，但不是「生成」——ffmpeg amix/concat 可做（`compose.ts` 的 `buildAmixArgs` 已有半成品与测试） | `compose.ts`、`host-tools.ts`（compose_video）、可选音频节点 UI（用户上传/指定 BGM 文件） | 分三步：① compose_video 支持传入 Master Audio（音频节点或文件），装配时以 Master Audio 音轨为准、视频段静音拼接或 ducking；② skill 教 Agent 在分镜前先锁定全局 BGM 描述并落成节点；③ 音频节点 kind=audio 最小闭环（上传/列表/选择），替代 bgmNodeId 传文件名的临时态 |
| CV-042 | 待处理 | P2 | 风格化画面文字能力未规划：H3 可把文字直接生成进画面（标题、霓虹灯牌、歌词字卡，MV skill 的 Typography 体系专门教这个——文字是空间化设计元素而非字幕条），我们的分镜表/文案结构里没有「文字层」概念 | `skills/creation-spec.ts`（分镜表增加文字列或文案节点结构） | 分镜表格式增加可选「画面文字」字段；借鉴 MV skill 的 Typography 三卡隔离原则（字体卡只管文字包装风格，不污染人物/场景卡；文字不遮眼部和口型）；对白精准字幕（SRT 转写+烧录）明确排除在 H3 能力外，等真需求再立项 |
| CV-043 | 待处理 | P2 | Ref2VA 的音频参考与音频复用能力未利用：音频参考（≤3 段，仅风格/音色/节奏参考，不能独立输入）、音频复用（视频编辑/续接场景保留原声）。对应场景：用户上传参考视频时抽其音轨做 BGM 风格参考；续接生成保留原片声音 | `generate.ts`（Drama 请求体）、`video-style.ts`（抽帧已有，抽音轨同理 ffmpeg 可做）、skill | 远期记录。前置依赖：CV-039（提示词规范）与 CV-040（Master Audio）落地后再评估 |
| CV-046 | **已修复·待验收** | P1 | canvas-studio 项目注册表落盘在 `$DSH_HOME/canvas-studio/`（home 根下、**跨 profile 共享**），且为「常驻内存 + 原子写」：若用户把插件装进两个共享同一 DSH home 的 DSH 实例（如已有 web 端 server + 本项目桌面壳），后写方会以自己的内存副本整表覆盖 `projects.json`——先写方新建的项目记录从注册表消失（目录仍在磁盘，表现为「项目丢了」）。原子写只保证文件不损坏，防不了两份内存态互相覆盖（2026-09-01 与用户讨论 DSH 并存冲突时确认） | `projects.ts`（registry root 默认 `dshHomePath('canvas-studio')`） | 择一：① 注册表迁到 profile 级目录 `$DSH_HOME/profiles/<name>/canvas-studio/`，首启检测旧位置存在而 profile 目录无注册表时自动迁移；② `projects.json` 加文件锁 + 写前重读合并。单实例为主流场景，紧迫性低，但属数据完整性问题，先于 CV-047/048 处理 |
| CV-047 | 待处理 | P2 | 本机已有另一 DSH 实例时端口静默漂移：桌面壳 webserver 从 43120 起占用即向后重试 32 个端口，被占时**无任何提示**地换端口，用户不知道同时开了两个 DSH；共享 `~/.dsh` 时 profile 重名（本项目默认 profile 名 `desktop`）还会互相改写 bundle 清单 | `dsh-plugin-desktop/src/desktop-port.ts`、`profile-manager.ts` | 桌面壳启动时端口退避命中后，托盘/通知提示「检测到其它 DSH 实例，已改用端口 N」；文档同步说明并存行为（与 CV-048 一并做） |
| CV-048 | 待处理 | P2 | 缺少「本机已有 DSH」的官方指引：DSH home 解析优先级为 显式配置 > `$DSH_HOME` > `~/.dsh`，用户已有的 DSH 与本项目默认共享 `~/.dsh`，用户不知道如何隔离（2026-09-01 与用户讨论 DSH 并存冲突时确认） | `docs/faq.md`、`docs/user-guide.md` | 文档写明：本机已有 DSH（web 端或客户端）时，建议为本项目设置独立 `DSH_HOME` 环境变量，或使用不同 profile 名；说明哪些目录是 home 级共享（canvas-studio 注册表）、哪些是 profile 级隔离 |
| CV-049 | 已完成·待验收 | P1 | ask_user_choice 的自由输入是 opt-in：只有模型显式传 `allowFreeText=true` 才渲染自由输入框，模型没传时（常见情形）用户面对不合意的选项**没有「都不满意，自己输入」的出口**，只能硬选一项或去对话区绕开卡片作答，HITL 卡片反而成了障碍（2026-09-01 用户提出） | `host-tools.ts`（工具 schema 默认值）、`contracts/project.ts`（allowFreeText 契约）、`client/question-capture.tsx`（L170 条件渲染） | 把自由输入改为默认开启：① 契约语义反转——`allowFreeText` 缺省视为 true，仅显式 `false` 时隐藏（模型少传一个参数，出口永远在）；或 ② 保留 flag 但卡片常驻一个「都不合适？自己说」折叠入口展开输入框。推荐 ①，改动最小且与「提问必须留出口」原则一致；skill 同步删掉「开放要素才传 allowFreeText=true」的教导 |
| CV-052 | 已修复·待验收 | P0 | 模式切换按钮把工作流状态误翻回 `drafting`：`routes.ts:644-648` 三条判据只看 `current.state` 与传入 mode，**从不比对 `current.mode`**，于是「点当前已激活的模式」也被当成「切换模式」执行。最严重格：`confirm` + `keyframe_review` 点「逐步确认」→ state 翻 `drafting` → 确认条消失 + AI 已结束回合在睡 + setMode 无 wakeAgent = **死锁**。已核实 `tests/` 对 setMode 零覆盖（修复前） | `routes.ts`（setMode → 纯函数化）、`contracts/project.ts`（新增 `resolveSetModePatch`）、`StudioFrame.tsx`（按钮 disabled）、`styles.ts`（`:disabled` 样式）、`tests/workflow-mode.test.mjs`（新增 7 用例） | ✅ 全部落地：短路分支 + disabled 防御层 + 单元测试；验证链全绿（153/153）。完整行为矩阵与 UI 验收法见 [redo-redesign-plan.md](./redo-redesign-plan.md) §A2-1 |
| CV-056 | 已修复·待验收 | P1 | 切模式解除等待后**不唤醒 agent**：`awaiting_approval` / `keyframe_review` 切「放手跑」→ state 置 `executing`，但 `setWorkflowMode` 不含 wakeAgent，而 AI 在调完 submit 后已结束回合静默等待。结果状态条显示「制作中」，流程实际停摆 | `client/index.ts`（setWorkflowMode 补 before.state 快照 + wakeAgent('继续')） | ✅ 落地。与 CV-052 同文件同批修 |
| CV-057 | 已修复·待验收 | P1 | 视频播放没有进度条也没有控制按钮（2026-09-01 用户提出）：双击视频打开的播放浮层（`VideoPlayerModal.tsx`）只有「点击画面切播放/暂停」，无进度条、无时间显示、无音量、无法拖动进度；画布内缩略视频同样全裸 | `VideoPlayerModal.tsx`、`styles.ts` | **硬约束**：Chromium 原生 controls 因「双击=元素全屏」shadow DOM C++ 路径不可用（CV-044 三次修复的最终结论），画布与浮层都不能挂 controls。修法：浮层内自绘迷你控制条——进度可拖（`timeupdate`/`seeked` 事件同步）+ 当前/总时长 + 播放暂停 + 音量，仍不使用原生 controls；画布缩略图维持无控件（点击/双击进浮层） |
| CV-060 | 已修复·待验收 | P1 | 应用名迁移半途、同一应用两个名字（2026-09-01 用户提出，附截图）：标题栏硬编码「DSH Desktop」（`ExtendedTitlebar.tsx:181`）、Electron `productName: 'DSH Desktop'`（`dsh-plugin-desktop/src/index.ts:394`），而托盘与更新模块已改叫 VideoBuddy（`tray-locale.ts:26-52`、`updates.ts`、`windows-volume-diagnostics.ts:104`）。用户要求标题栏显示自己的应用名 | `dsh-plugin-desktop/src/index.ts:394`、`client/ExtendedTitlebar.tsx:181`、`recovery-copy.ts`（大量 "DSH Desktop" 字样）、README/日志路径文案 | 统一为 **VideoBuddy**：① productName + 标题栏 span；② 全量排查 `DSH Desktop` 残留（recovery-copy.ts、日志文档、e2e 脚本）；③ 核对 userData 目录迁移影响（`~/Library/Application Support/` 下旧 DSH Desktop 目录与新 VideoBuddy 目录，见 canvas-studio-handoff.md §6 重启锁清理路径）；④ 应用名变更影响 Electron 单实例锁与崩溃目录，验收时重点回归首启/重启 |
| CV-058 | 已修复·待验收 | P2 | 上传图片没有任何选项（2026-09-01 用户提出并核实属实）；`StudioFrame.tsx:535` 引导文案写着「上传图片时勾选『设为参考图』」——**实际交互里不存在这个勾选框，文案在撒谎**。**已拍板：只改文案（方案②），不加选项浮层**——上传功能后续在 LLM 编排里已有。上传链路核实：**双写** = 本地落盘 assets（`generate.ts:330`）+ 同步传 Drama 拿 filename（`generate.ts:333-334`）落节点，生成无需再传；副作用 Drama 不可用时整次上传失败、节点也不落（all-or-nothing，暂不动） | `StudioFrame.tsx:225-251`（handleUploadImage）、`StudioFrame.tsx:535`（撒谎文案） | 535 行文案改成真实路径：「上传后在详情面板点『标记为参考』」。零风险小改 |
| CV-059 | 已修复·待验收 | P2 | 工具栏图标化改版（2026-09-01 用户提出，附截图）：① 去掉设置按钮；② 整理布局 / 显示图层 / 显示小地图 三按钮移到工具栏最右（原设置位）；③ 全部图标化，不用文字（保留 title / aria-label）。**已拍板：设置入口用 app 左下角的全局入口，画布设置按钮直接删**。注意：删除前确认画布设置弹窗与全局设置是否同一实例 / 同一持久化路径，弹窗组件保留供全局入口复用 | `CanvasToolbar.tsx`（TOOLBAR_VISIBILITY + 按钮渲染）、`styles.ts` | 图标用内联 SVG（对齐现有齿轮 stroke 风格，`strokeWidth=2`、16px），不引图标库 |

## P1 — 功能缺口（核心工作流）

| ID | 状态 | 优先级 | 问题 | 涉及文件 | 改进意见 |
| --- | --- | --- | --- | --- | --- |
| CV-094 | 已修复·待验收 | P1 | **创作任务里 skill 调度顺序错乱**（2026-09-03 会话复盘发现，[会话日志复盘](#)）：用户「生成一段折纸动画」的 12 步会话里，模型先凭直觉连问 3 轮 `ask_user_choice`，step4 才加载创作总纲，**风格 skill 全程一次未加载**。偏差：漏第①步形态题、②时长+③画幅合并成一问、风格题自造选项未走「4a 大类 → 4b 逐字预设名」两级追问、跳过 `prompt_enhance`、`image2vl` 500 静默吞掉。根因：总纲 catalog `description` 是软约束，模型可晚加载甚至不加载；风格题一错就再也映射不到预设名 → 风格 skill 加载不出来 → 上游 STEP 流程与预览 GIF 全部落空 | `skills-local/canvas-studio-creation/SKILL.md`（自研，可改）+ `skills/` 同步副本 | ✅ 已落地：① `description` 改硬指令——「第一个动作必须是调用 skill(name=canvas-studio-creation) 加载本规范，严格先于一切提问与工具调用，禁止凭直觉先行澄清」，并点明含「风格预设两级追问（决定加载哪个风格 skill）」（243 字，未触发 500 字截断）；② 正文开头加「加载时机铁律」自救条款——晚加载时立即停止即兴流程、说明后从需求澄清第①步重来。**未做的更强手段（待拍板）**：Host 侧首轮自动注入总纲（确定性最高，需动代码）；`ask_user_choice` 风格题在 Host 侧校验预设名白名单（成本较高） |
| CV-095 | 已修复·待验收 | P1 | **图像侧提示词规范 skill 缺失**（2026-09-03 调研，[image-skill-research.md](./image-skill-research.md)）：视频侧有 `h3-prompt-writing` + 8 个 H3 风格 skill，图像侧长期空白，出图提示词全靠模型即兴发挥。且两条链路的写法**互斥**却无文档区分：文生图（Z-Image-Turbo）**忽略 negativePrompt、无 CFG/步数可调**，约束只能正向写；图生图/改图（Qwen-Image-Edit 路线）是**指令式**而非描述式，必须带保留子句。附带冲突：`image_generate` 暴露的 `negativePrompt` 对文生图无效；`style=anime` 传参考图静默回退写实；`inpaint`/`style_transfer` 禁用导致通用改图无入口 | `skills-local/{z-image-prompt-writing,qwen-image-edit-writing}/SKILL.md`（新增）+ `skills/` 同步副本 + `skills-local/canvas-studio-creation/SKILL.md` + `src/skill-catalog.ts` | ✅ 已落地（方案 A，用户拍板）：拆两个 skill 按需加载——`z-image-prompt-writing`（1.9KB：九段式、负向→正向改写表、打光/文字渲染词表、画幅尺寸、`anime` 仅纯文生图）+ `qwen-image-edit-writing`（2.5KB：四段式、分步链式、多参考图分工、6 个场景模板、禁用工具替代路径）；总纲加「图像提示词写法」调度小节 + 两条硬约束（文生图禁传 negativePrompt；inpaint/style_transfer 禁调）；catalog 补两条 `prompting` 条目且 `hidden: true`（对标 h3-prompt-writing）。**用户拍板**：inpaint 不解禁、negativePrompt 参数保留但 skill 内禁止使用、prompt_enhance 由模型自决。**待确认**：`image2image` 端点底层模型（代码注释与 OpenAPI 均未标注，多参考图输入特征指向 Qwen 系列，需后端确认） |
| CV-097 | 已修复·待验收 | P0 | **skill description 静默截断（SK-04）+ 路由不可观测（SK-08）**：`minimax-skills.ts:86` 注册时对 description 硬截 500 字符且全链路无日志，实测 13 个 skill 中 **7 个**正被砍（papercraft 丢 414 最多），砍掉的恰是排在最末的负向路由语 → 模型选 skill 的唯一依据残缺。同批解决「路由无法观测」：注册后无任何汇总日志，只能事后翻 session jsonl 复盘（CV-094 即如此才发现） | `src/skills/minimax-skills.ts` / `tests/minimax-skill.test.mjs` | ✅ 已落地（SK-04 + SK-08 合并一次改动）：`DESCRIPTION_LIMIT = 1024` 具名常量（实测 max 914 + 100 buffer）+ 纯函数 `truncateDescription(raw, limit)`（恰好等于上限不截断）+ 截断时 `console.warn` 报丢弃字符数 + `collectSkillStats()` 供注册流程与单测共用 + 注册完成输出汇总日志 `skills registered: N total, M description truncated`。测试追加 3 用例（纯函数三态 / 长度快照哨兵 / 日志行为），原有「≤500」断言改为引用常量。实测截断 **7/13 → 0/13**，test:smoke **198/198**。**遗留**：SK-08 第 2 项「加载时 info 日志」未实施——`@deepseek-ai/dsh-skill` 仅暴露 `skills/change`、无加载事件，需 hook `SkillRegistry.load()` |
| CV-098 | 已修复·待验收 | P1 | **创作任务路由硬指令常驻 system prompt（SK-01）**：CV-094 把「先加载总纲再澄清」写进总纲 description 后实测有效，但依赖模型主动读 catalog 摘要，且硬指令挤占 description 的字符配额（该配额应留给负向路由语）。作为 CV-094 复盘时记录的「更强手段」之一落地 | `src/skills/routing-prompt.ts`（新增）/ `src/index.ts` / `tests/skill-routing-prompt.test.mjs`（新增） | ✅ 已落地：注册常驻小节 `canvas-studio:skill-routing`（order 150，工具指引带），指令为**条件触发式**——仅创作类请求生效 + 非创作请求豁免 + 「不改变身份」声明；因 canvas-studio 是全局插件（bundle patch 顶层插入），条件式措辞保证非创作会话不受影响。测试 6 用例含总纲 skill 名存在哨兵与 lib 产物防漏 build。test:smoke **204/204**。**桌面验收**：重启后非创作会话（如「帮我写个周报」）应不受路由指令干扰；创作会话第一动作仍为加载总纲 |
| CV-099 | 已修复·待验收 | P1 | **创建项目预置画幅 + 目标时长**：创建弹窗收集画幅/时长（可留空），生成侧兜底（显式参数 > plan > 全局）+ 动态 system prompt 注入（`src/plan-prompt.ts`，`agent/created` 预热），总纲澄清 6 问 → 3 问 | `src/contracts/project.ts` / `src/projects.ts` / `src/routes.ts` / `src/generate.ts` / `src/plan-prompt.ts`（新增）/ `src/index.ts` / `src/client/{api,contracts,index}.ts` / `ProjectList.tsx` / `styles.ts` / `skills-local/canvas-studio-creation/SKILL.md` | ✅ 已落地：`StudioProject.plan` 零迁移；单镜时长钳到 ≤ 目标总时长；镜头数 ≈ 总时长÷8–10s 改推导。验证链全绿（test:smoke **272/272**）。**桌面验收**：新建项目选 9:16 / 30s → 只说「做个咖啡广告」→ 出图为竖构图且全程不问画幅/时长/节奏/受众 |
| CV-100 | 已修复·待验收 | P1 | **剧本创作前置（风格 skill 后）+ script_review 审批门禁**：新状态 `script_review` + 工具 `write_screenplay`（原地更新）与 `submit_screenplay_for_approval`（auto 放行）；批准回 **drafting**（绝不设 executing，否则分镜审批被跳过）；总纲「剧本创作」节声明唯一节点（= 上游故事大纲/story-outline，禁止另建） | `src/contracts/project.ts` / `src/host-tools.ts` / `src/routes.ts` / `src/client/{api,contracts,index}.ts` / `StudioFrame.tsx` / `skill-catalog.ts` / `skills-local/canvas-studio-creation/SKILL.md` | ✅ 已落地：剧本落点=风格确定并加载 skill 之后（上游自带大纲步骤，前置会撞双节点）；驳回意见随唤醒消息定向转述。验证链全绿（test:smoke **272/272**）。**桌面验收**：逐步确认模式完整走一遍——剧本节点只出现一个且在风格确定后；批准后分镜审批仍生效；auto 模式剧本门禁直接放行 |
| CV-005 | 待处理 | P1 | 血缘连线**只能加不能删**：手动 link 后无 UI 移除单条边（sourceIds 无编辑入口）；Agent 画错血缘同样无法修正 | `CanvasEdges.tsx`、`CanvasContextMenu.tsx`、`project-store.ts` | 点击边高亮 + Delete 删除；或详情面板加「来源列表」逐项 ×。见决策点 D3 |
| CV-006 | 待处理 | P1 | compose **无法排除片段**：导出固定取时间轴上全部 kind=video（StudioFrame handleComposeExport）；废弃片段必须删除才能排除；工具支持 `bgmNodeId` 但 UI 无 BGM 选择器（代码注释自认「第一版从简」） | `StudioFrame.tsx`、`CanvasTimeline.tsx` | 时间轴 chip 加**勾选态**（默认勾选），compose 只取勾选项；工具条加 BGM 节点选择器 |
| CV-007 | 待处理 | P1 | 时间轴语义混乱：便签/文本/分镜表 chip 与媒体混排、可拖排序，与成片无关；只有「视频片段 N」计数，**无总时长**显示 | `CanvasTimeline.tsx` | 非媒体节点折叠/置灰不可拖；chip 显示各自 duration；工具条显示总时长 Σ。与 CV-006 一并做。见决策点 D2 |
| CV-008 | 已修复·待验收 | P1 | 多选是半成品：只能 ctrl 点选；**拖拽只移动被按下的单个节点**（gesture 仅带单 nodeId）；无框选（marquee）。（注：group 拖动带动 children 经代码核实 `moveNode` 已实现，原记载过时） | `CanvasSurface.tsx`（Gesture / onNodePointerDown / onPointerMove / onPointerUp）、`project-store.ts` | 已实现：① 多选整体拖拽——node gesture 捕获选区全员起始位置（`origins`，过滤组内成员防双重位移），以被按节点为主做 snap、校正量均摊到全体；② marquee 框选——空白左键拖拽画选框（屏幕层 `.csMarquee`），抬起按世界坐标矩形相交落选，Ctrl/Cmd 叠加既有选区（先清空再逐个加入，规避 selectNode 翻转语义 bug），单击=清选，拖出容器取消；③ 平移改为 Shift+左键 / 中键 / 滚轮承担 |
| CV-050 | **已修复·待验收** | P1 | 打回后 AI 重新提交分镜，`buildShotCards` 无脑追加整套新卡、旧卡原地不动 → 打回两次画布上三套同名分镜卡，且下游关键帧的 `shotRefs` 可能连到旧卡（CV-026 的已知遗留项，2026-09-01 正式立条） | `host-tools.ts:262`（buildShotCards）、`:704`（submit）、`:763-764`（落卡） | 按镜号匹配已有卡，复用 id 与位置只更新文案；新镜号新建，多出的旧卡保留不动。**D4 已于 2026-09-01 拍板为方案 A**（不静默删旧卡，避免下游 `shotRefs` 断链）。详见 [redo-redesign-plan.md](./redo-redesign-plan.md) §A1 |
| CV-051 | **已修复·待验收** | P1 | 关键帧阶段只有「确认关键帧」没有「打回」：整体不满意只能逐张右键重做（AI 不知情）或去对话里说 | `StudioFrame.tsx:717-723`、`routes.ts:627-629`、`client/index.ts:356-360` | 复用分镜审批条的意见输入框，新增「打回重出关键帧」→ 状态回 `executing`（放行 image_generate）+ 带意见唤醒 AI。低风险：`image_generate` 本就不在闸门拦截名单。详见 [redo-redesign-plan.md](./redo-redesign-plan.md) §A2-2 |
| CV-054 | 待处理 | P1 | 重做没有后悔药：重试成功后主机整体重载画布，撤销栈被覆盖（`updateNode` 不进 history），换回旧版无门 | `contracts/canvas.ts`、`NodeDetailDrawer.tsx`（CV-194 起取代 `LayerDetailPanel.tsx`） | 节点加 `previous?: { url, filename, generationPrompt }`，重做前压栈，详情面板给「撤销上次重做」。比 revisions 数组（cap 5）轻得多。**D5 已于 2026-09-01 拍板为方案 A（单版本回退）**，不做 revisions 数组 |
| CV-067 | 已修复·待验收 | P1 | 竖屏成片在画布被横屏框 cover 裁切（用户实测截图反馈）：`compose_video` 成片节点落盘硬编码 `COMPOSED_SIZE 260×180`（横屏占位），忽略已探明的真实分辨率（480×864 只进 mediaWidth/mediaHeight）；9:16 视频塞 260×180 框被 `object-fit: cover` 上下裁切。客户端 `onMediaNatural` 异步校正兜底不稳——依赖 `<video onLoadedMetadata>` 时序，节点刚落盘、媒体未加载完的窗口期就是横框 | `compose.ts`（`COMPOSED_SIZE` → 按分辨率换算）、`src/canvas-aspect.ts`（**新增**，分辨率→显示框唯一事实来源）、`generate.ts`（删本地 previewSizeOf 改 import）、`StudioFrame.tsx`（删 longSide480，上传探测/媒体校正走共享）、`tests/canvas-aspect.test.mjs`（**新增** 7 例） | 落地（方案 B 逻辑收敛）：① 新模块 `canvas-aspect.ts` 统一「真实分辨率 → 画布显示框」规则（长边 480、1:1→420×420、短边 60 地板，对齐验收用例 I-2）——此前三处重复实现规则漂移（generate 1:1→420 vs StudioFrame 1:1→480）；② `compose.ts` 成片落盘按真实分辨率换算（1:1→420、9:16→267×480、16:9→480×270），缺分辨率才回退 260×180 占位（媒体加载校正兜底）；③ 上传探测 `probeImageDisplay` / 媒体加载校正 `onMediaNatural` / `generate.ts` 全部改走共享函数；④ `tsconfig.client.json` include 追加 `src/canvas-aspect.ts`（client 引根级模块铁律） |
| CV-068 | 已修复·待验收 | P1 | 主模型无视觉（qwen3.8-27b-mtp 等文本模型），agent 在生成「视觉预览」等中间产物后试图**直接读本地图片路径自查效果** → DSH 运行时把 png admit 成 image block 提交给模型，能力校验抛 `model does not declare image input`（用户实测截图反馈，3 连错）。架构上视觉理解走专用工具（`upload_image` 拿 Drama 服务器 filename → `image2vl` 分析）；报错串不在本项目源码（DSH 运行时），= agent 绕过专用通道直接读图，SKILL.md 旧护栏（「不要用文件读取工具打开图片」）太轻未拦住 | `skills-local/canvas-studio-creation/SKILL.md` + `skills/canvas-studio-creation/SKILL.md`（双副本，运行时读 skills/、sync 脚本从 skills-local 拷贝）、`host-tools.ts`（image2vl description）、`tests/skill-guardrail.test.mjs`（**新增** 3 例） | 落地（方案 A 提示词护栏，用户拍板）：① SKILL.md 核心规则升级显式禁令——无视觉能力声明（禁止 file_path / 图片 URL / 内嵌引用 / 附件全部读图变体）+ 产物 url 用途澄清「只给展示与 upload_image，非视觉输入」+ image2vl 唯一合规通道（`upload_image(imageUrl=url)` → `image2vl(filename=…)` 前置步骤）；② `image2vl` description 补工具级护栏；③ 守卫测试断言双副本一致 + 禁令关键词在位 + lib 产物含护栏（防漏 build） |
| CV-070 | 已修复·待验收 | P1 | 技能广场卡片**动态预览**缺失（竞品对标 DA-1，2026-09-02 用户截图）：竞品每张技能卡头图是 GIF/视频演示（在动），我们是静态 hue 渐变 + 图标，观感差距最大。**素材已就绪**：`assets/style-demos/` 8 个风格 GIF 与 skill 目录**同名一一对应**（10 个 skill 中 8 个可立即用） | `src/skill-catalog.ts`（entry 增 `demo?` 字段）、`SkillCard.tsx`、`styles.ts`、assets 打包链路确认 | ✅ 已落地后**行为调整 2026-09-02**：初版「hover 懒注入动图、不 hover 不下载」；用户体验反馈「动画应默认显示，hover 只叠加菜单」，改为 **demo 动图默认渲染**（`loading="lazy"` 控加载），hover 仅由 CSS 在其上叠加操作菜单（不再把静态切换动图）；`prefers-reduced-motion` 降级静态渐变；无 demo 回退静态渐变；**缩略图高度 64→110px 放大动效展示**；测试守卫 demo GIF 文件真实存在。完整方案见 [competitor-analysis.md](./competitor-analysis.md) DA-1 |
| CV-072 | 已修复·待验收 | P1 | 技能广场缺**搜索框**（竞品对标 DA-3）：竞品右上「搜索 Skill...」，skill 一多搜索是刚需；我们 10 个尚可但应先占位 | `SkillMarket.tsx`、`styles.ts` | ✅ 已落地：header 搜索框，纯前端 title/summary/name 子串过滤（大小写不敏感），与分类筛选叠加（分类内再搜）；零新依赖 |
| CV-079 | 已修复·待验收 | P1 | 生成产物**自动编组**缺失（竞品对标 DA-10，用户口述点 1）：竞品按段编组（组标题栏 + 折叠 + 组内添加），我们生成物全平铺在画布，素材归类靠手动。前置 CV-008 已随本批一并完成 | `generate.ts`（`attachShotGroup`）、`CanvasSurface.tsx`（CV-008 多选/框选） | ✅ 已落地：`attachShotGroup` 纯函数——有分镜卡血缘时并入「分镜 N · 素材」组（组 sourceIds 记分镜卡 id，后续同镜产物据此复用；组框随成员包围盒扩展），无分镜卡保持 appendCanvasNode 旧行为；存量节点不追溯拉组。+2 单测。折叠组视图未做（后续评估） |
| CV-080 | 已修复·待验收 | P1 | 生成节点**图层名模糊**（竞品对标 DA-11，用户口述点 2）：`generate.ts` 落盘的媒体节点大多无 title，图层名回退 kind/operation 泛化标签（「图片」「视频」），同名不可区分；仅分镜卡/抽帧/风格归纳/文案少数有命名 | `generate.ts`（命名 helper `mediaNodeTitle`/`promptSummary`）、storyboard_split（`单镜 N`） | ✅ 已落地：统一命名规则——① 关键帧/视频 = `分镜 N · 关键帧/视频`（shotRefs 解析取镜号），无镜号取 prompt 前 12 字摘要；② upload_image = 原文件名（客户端 addImportNode 已有）；③ 抽帧/风格归纳/compose 已有名保持；④ 单镜拆分命名 `单镜 N`。LayerPanel 与节点标题读同一 title 天然同源，无契约变更。+1 单测 |
| CV-089 | 已修复·待验收 | P1 | 画布**选中/拖动视觉多项缺陷**（用户 2026-09-03 多轮实测反馈，逐条累积）：① **点选单张图拖动 → 其余全部节点被压暗「蒙一层」**（本轮核心）；② 选中态像「蒙了一层蓝」；③ 视频/图片浮层元信息条（分辨率+时长）整行消失；④ 双击节点无反应（视频+图片同一根因）；⑤ 节点无分辨率信息 | `styles.ts`、`CanvasSurface.tsx`（Gesture / primaryDragId）、`CanvasNode.tsx`、`VideoPlayerModal.tsx`、`ImagePreviewModal.tsx` | ✅ 已落地：**① dim 语义接反（根因）** —— `.csCanvasSurface[data-dragging="true"]` 把非被拖节点压到 opacity 0.55、非主拖选中节点压到 0.85，而 `data-dragging` 是在 `onNodePointerDown`（**节点拖动手势**）置上的；dim 的合理语义是「框选时区分命中/未命中」，两者接反，于是点选单张图拖动就把整屏压暗。修复 = **移除 dim 规则**，拖动时不再改任何节点不透明度，只给主拖节点 `csNodePrimary`（z-index:3 + 加粗描边）区分「主/随从」。顺带修掉一处隐性 bug：`primary` 原本从 `gesture.current`（ref）读，**ref 变更不触发 re-render**，渲染期永远拿到上一次渲染的值；改为 `primaryDragId: string \ | null` state（`draggingNodeIds` 数组 state 收敛而来）。**② 选中态** —— 原用 `--dsw-alias-interactive-bg-active`（带透明度的浅蓝）叠大节点上就是蒙层，改 `--cs-accent` 实色描边 + `--cs-accent-soft` 外光晕。**③ 元信息条** —— `{dims !== null && ...}` 条件渲染，metadata 未就绪时整行不渲染（看着像没做）；改**始终渲染**，未就绪显示 `— × —` / `加载中…`。**④ 双击失效** —— pointerdown 立即 `setPointerCapture` 致 `mousedown/mouseup` 被 retarget 到捕获元素（容器），dblclick 的 target 因此变成容器、节点上的 `onDoubleClick` 收不到；容器自己的双击 fit 一直正常，正是「双击空白有反应、双击节点全挂」。改为 `armPointer`（只记 pointerId）→ **首次 move 才 `ensureCaptured()`**，并加 `DRAG_THRESHOLD = 3` 拖拽阈值（顺带消灭「双击手抖 1px → 产生位移 + 一条空 undo 快照 + 一次全量写盘」）。**⑤ 其余** —— 分辨率角标（右下，与左下 CV-083 时长对称）、`.csNodeLinkHandle` 默认 `opacity:0` 改 hover/选中才显（原每个媒体节点右缘常驻 12px 圆点，节点一多也是「蒙一层」观感来源）、删 `csNodeRing` 死元素（空样式零视觉）。**编号纠正**：这批改动曾在代码注释里误用 CV-072/CV-073（已归「技能广场搜索 / 我的 Skill」），已按行号精确改回 CV-089（23 处；SkillMarket / activeSkills 相关 11 处保留不动） |

## P2 — 信息展示优化

| ID | 状态 | 优先级 | 问题 | 涉及文件 | 改进意见 |
| --- | --- | --- | --- | --- | --- |
| CV-074 | 已修复·待验收 | P2 | 技能广场无「官方精选」分区（竞品对标 DA-5）：竞品分「精选大卡区 + 其他列表区」两级；我们平铺。catalog 的 `featured` 字段**已有但未消费** | `SkillMarket.tsx`、`SkillCarousel.tsx`（lobby 推荐位对齐 featured） | ✅ 已落地：「官方精选 / 其他技能 · N」两级分区（仅「全部 + 无搜索 + 未勾过滤」时生效，其余场景平铺）；纯渲染层分组，零数据改动 |
| CV-083 | 已修复·待验收 | P2 | 视频节点无**时长角标**（竞品对标 DA-14）：竞品节点左下角 `0:16` / `0:56`；我们缩略图零信息。**内联完整控件明确不做**——与 CV-044「原生 controls 被禁」结论冲突（画布节点尺寸小自绘控件拥挤），完整播放仍走 CV-057 浮层；下载角标不做（右键+详情已有下载，避免入口重复） | `CanvasNode.tsx`（时长角标）、`canvas-aspect.ts`（`formatMediaDuration`）、`styles.ts` | ✅ 已落地：loadedmetadata 现算「m:ss」角标左下角显示。**实现决策：不落盘 duration**——重载后重新读 metadata 时长自然恢复，省一条契约字段（与原方案落盘的差异）。+1 单测（8 例） |

## P2 — 交互补全

| ID | 状态 | 优先级 | 问题 | 涉及文件 | 改进意见 |
| --- | --- | --- | --- | --- | --- |
| CV-021 | 待处理 | P2 | 删除被血缘引用的节点时无提示，下游 sourceIds 静默悬空（渲染不报错但信息链断裂） | `StudioFrame.tsx`（handleDelete）、`project-store.ts` | 删除前检测是否有下游引用并提示；或删除时级联清理子节点 sourceIds |
| CV-053 | 待处理 | P2 | 审批条不醒目：只在 `awaiting_approval` / `keyframe_review` 时出现，挤在工具栏下面一行，无高亮无角标，用户注意不到（2026-09-01 用户反馈「打回重做不大会用」的根因之一） | `StudioFrame.tsx:699`、`styles.ts` | 有待处理动作时审批条加高亮边框 + 主题色角标 + 未读数；无待办时保持现状。详见 [redo-redesign-plan.md](./redo-redesign-plan.md) §A3 |
| CV-055 | 待处理 | P2 | 重做不通知下游：换了关键帧，引用它的视频不会有任何「已过时」标记，全靠用户自己记住哪些要重生成 | `project-store.ts`、`CanvasNode.tsx` | 重做落盘后沿 `sourceIds` 反向 BFS 给下游打 `staleAt`，节点显示「上游已更新」。只做标记不做自动级联 |
| CV-069 | 已修复·待验收 | P2 | 用户信息区缺失（竞品对标 MiniMax Design，2026-09-02 用户截图）：竞品左栏底部有用户卡（头像/昵称/UID），点击弹个人信息面板（账号身份 / 积分余额 / 订阅 / 设置组 / 帮助组）；canvas-studio 左栏底部为空，且 CV-059 拍板「设置入口 = app 左下角全局入口」后画布侧缺插件内设置落点。**用户已拍板四项**：① 主题/设置接真实功能；② 记忆管理/教程/更新日志等未规划条目保留入口挂「待接入」（尽量完整复刻竞品结构）；③ 用户卡三态常驻（lobby / lobby-pending / work）；④ 假数据用中文创作人设 | 新增 `client/UserCard.tsx`、`StudioFrame.tsx`（左栏底部挂载）、`styles.ts`（csUser* 系列）、假数据常量（`brand-copy.ts` USER_MOCK） | ✅ 已落地：`UserCard.tsx`（首字母 + 品牌色渐变 SVG 头像 + 点击向上弹 popover；关闭语义复用 CV-037 教训：mousedown 内部放行 + Escape）；主题三态 = 真实 ctx.theme（与 SettingsModal ThemeSection 同款 setTheme），设置 = 打开 SettingsModal（恰是 CV-059「左下角」落点，与 app 级入口互补）；积分/订阅/记忆管理/教程/更新日志 = reserved 入口挂 `csReserved` 待接入角标，「接入飞书/微信」未接入 badge；`USER_MOCK` 假数据收敛 brand-copy.ts（CV-088 同源）；**审查回填 2026-09-02**：面板打不开（toggle 内嵌 setPanelPos 副作用被丢弃）已修、用户条「双按钮」回退为单按钮、`.csUserPanel` 的 `bg-l1` 无效令牌已验证清空（详见 STATUS §8）；**二次回填 2026-09-02**：移除 ProjectList 遗留的左下角 ⚙ 设置图标（`csProjectListFooter` / `csProjectSettingsIcon`，与面板内设置入口重复），面板打不开经 React 18 最小复现断定是桌面加载旧 lib、需重启生效（详见 STATUS §8） |
| CV-071 | 已修复·待验收 | P2 | 技能卡无**悬停操作浮层与详情**（竞品对标 DA-2）：竞品 hover 头图浮现「查看详情 / 下载 Skill」双按钮；我们仅常驻一个「使用」按钮 | `SkillCard.tsx`、`SkillMarket.tsx`（详情浮层）、`styles.ts` | ✅ 已落地（**2026-09-02 行为调整**：悬浮层由「仅查看详情」扩为「使用 + 查看详情」双按钮，ghost 变体区分主次；配合 CV-070 默认动图叠加而非切换）：hover 浮层 `csSkillHover`（纯 CSS opacity 过渡 + `pointer-events` none→auto）+ 二级详情弹窗（色块缩略图 + 标题 + H3 角标 + 分类 + 说明 + 注册名 + 使用按钮；Escape 先关弹窗再关广场、backdrop 点击关闭） |
| CV-073 | 已修复·待验收 | P2 | 技能广场无「**我的 Skill**」视角（竞品对标 DA-4）：竞品双 Tab 分「发现 / 我的」；已激活技能在广场内不可见，管理只有 work 态 chips 行 | `SkillMarket.tsx`、`StudioFrame.tsx`（接线 activeSkills / onDeactivate） | ✅ 已落地：侧栏「我的 Skill」视图（带计数），清单 = activeSkills 条目（标题取 catalog、未收录显示注册名）+ × 卸载（复用 CV-066 deactivateSkill 链路，store 即时更新 + skills.json 落盘）；lobby 态无项目隐藏入口；空态引导 |
| CV-081 | 已修复·待验收 | P2 | 文本类节点**选中后内容不可滚动**（竞品对标 DA-12，用户口述点 3）：`.csNodeBody` = `overflow:hidden + ellipsis`，长文（分镜表/脚本/文案）选中态也截断 | `CanvasNode.tsx`、`styles.ts`、`CanvasSurface.tsx`（wheel 豁免） | ✅ 已落地：`.csNodeSelected .csNodeBody` overflow-y:auto + overscroll-behavior:contain + thin scrollbar；Surface wheel handler 对「可滚动的选中正文 / textarea」放行（scrollHeight > clientHeight 才豁免，不劫持平移缩放），不可滚时画布行为不变 |
| CV-082 | 已修复·待验收 | P2 | 视频节点 **hover 不自动播放**（竞品对标 DA-13，用户口述点 4）：缩略视频全静态，播放只能双击进浮层 | `CanvasNode.tsx`、`styles.ts` | ✅ 已落地：hover 150ms 延迟 `muted + loop + play()`（防快速扫过抖动），离开 `pause()` + 归零；模块级单实例登记（同时只允许一个 hover 播放）；loading/失败/错误节点不播；`prefers-reduced-motion` 降级不自动播；卸载清理；双击仍开播放浮层（CV-044 语义不变） |
| CV-084 | 待处理（单独排期） | P2 | 无**制作计划阶段清单**（竞品对标 DA-15）：竞品右栏「制作计划 3/3 合成成片」步骤勾选 + 点击定位节点 + 收起，执行进度可视；我们执行进度是黑盒（只有 mode + 审批条 + loading 耗时） | `contracts/`（workflow steps 扩展）、`host-tools.ts`（按工具调用推导状态翻转）、`StudioFrame.tsx`（清单渲染 + `focusNodeId` 定位）、skill（规划产出结构化 steps） | skill 教 agent 规划时产出 steps（阶段名 + 状态）→ host 从工具调用序列自动翻转（storyboard→脚本、image_generate→关键帧、video_generate→片段、compose→成片）→ 工作流条渲染勾选清单 + 点击定位。动契约 + skill 联动，作为 optimization-plan「实时状态反馈」（仅设计）的**轻量替代**立项，单独排期。本批未动 |
| CV-087 | 待处理 | P2 | 项目列表无**封面缩略图**（竞品对标 DA-18）：竞品项目卡有封面 + 日期，扫视识别效率高；我们纯文字行 | `projects.ts`（entry 增 `coverUrl?`）、`generate.ts` / `compose.ts`（落产物增量写注册表）、`ProjectList.tsx`、`styles.ts` | ① 落产物时把最近媒体节点 url 增量写进 entry；② 打开项目时若无 cover，客户端从 canvas.json 找首个媒体节点补写一次；③ 行渲染缩略图 + 名称 + 日期；无产物回退首字母块（CV-069 同款）。**性能红线：列表页不得为拿封面全量加载所有 canvas.json**——增量写注册表是关键。本批未动 |
| CV-090 | 待处理 | P2 | **框选（marquee）交互不好用**（用户 2026-09-03 反馈，CV-089 排除 dim 干扰后仍存在）：① 拖拽过程中**看不到哪些节点会被框中**，只能松手后才知道；② 指针划出画布表面（如移到右栏/底部时间线）**框选立即取消**，无法跨列框大片区域 | `CanvasSurface.tsx`（Gesture marquee 分支、`onPointerLeave`）、`styles.ts`（`.csMarquee`） | 待做两项（**拍板后再动**）：① **命中预览高亮** —— 框选期间按当前矩形与世界坐标节点做相交判定，实时给命中节点加轻描边（Figma 行为），松手前就知道选了谁；② **给 marquee 加 pointer capture** —— 出界不取消、回界继续框，与 node/resize/link 三种手势一致。现状是 CV-008/CR-060 当时**刻意不给 marquee 捕获**（防误选），判据偏严苛。CV-089 已移除错挂在拖动上的 dim，本条是框选本身的独立改进 |
| CV-194 | **已完成·桌面验收通过**（2026-09-17） | P1 | 节点详情面板**三个同源问题**（用户 2026-09-16 反馈）：① 弹框出现在**屏幕最右缘、离节点很远**；② 详情难读（15 行平铺同权，11px `word-break: break-all` 挤在 label 后约 180px）；③ 改提示词只有一个**单行 `<input>`**。读码后确认三条同源 —— `LayerDetailPanel` 根本不是「节点的附属物」，而是 `.csDetailPanel` = `position: fixed; top: 64px; right: 12px; width: 320px` 的**屏幕坐标浮窗**：位置与节点坐标毫无关系，且必然盖住宿主右栏 | `src/client/canvas/NodeDetailDrawer.tsx` / `NodeActionBar.tsx` / `PromptEditor.tsx`（三者新增）、`src/node-params.ts`（新增）、`src/canvas-view.ts`（`nodeActionAnchor`）、`src/client/canvas/LayerDetailPanel.tsx`（**删除**）、`CanvasSurface.tsx`、`StudioFrame.tsx`、`styles.ts`、`scripts/preview-detail.mjs`（新） | ✅ 已落地（批次 1 = UI 骨架）。**容器改为「就近工具条 + 底部可拖高抽屉」**（用户拍板 **B 方案**；否 A 锚定浮动卡、否 C 右缘停靠。业界依据：React Flow `NodeToolbar` 不随缩放且仅单选可见、n8n NDV 侧边就地编辑且「聚焦」只对多行文本出现、NodeTool 右栏只做 Inspector、Grida section 化 + 失焦提交）。**两个几何决策都做成「不需要浮点账」的形式**：① 工具条渲染在 `.csCanvasLayer` **之外的兄弟层** ⇒ 尺寸天然不随缩放变形（**写进层内 = 0.4× 时按钮糊成一团**），位置由纯函数 `nodeActionAnchor` 现算（屏幕坐标 = 世界 × `scale` + `view` 偏移；上方装不下翻下方；两端按 `NODE_ACTION_MARGIN=8` 夹取；整块出视口 `visible=false` 不渲染）；② 抽屉挂 `.csCanvasBody` 内绝对定位 ⇒ **底边就是时间轴顶边、宽度就是画布宽**，「离节点远」「压宿主右栏」从几何上不成立。**工具条**：重试 / 改提示词 / 引用到对话（`hasActions` 为假时**整条退场**）；仅「单选 + 非手势中」出现（拖动/多选时不打扰）；`pointerdown` / `dblclick` / `contextmenu` 全 `stopPropagation` —— 不拦的话按按钮的**按下瞬间**选区就被清掉。**抽屉**：左「身份/变换」只读 + 右「内容」可编辑 + 底栏操作（重试 / 下载 / 打断 / 删除靠右）；上缘 6px 抓取带拖高（`MIN_DRAWER_HEIGHT=148`、`MIN_CANVAS_VISIBLE=180`），高度写 `localStorage['canvas-studio.detail-height']`（**设备级偏好，不进项目契约**），默认 320。**提示词三档**：就地自增高 → 抽屉右栏整栏 → 聚焦大窗（左「已保存基准」/ 右「编辑」并排；失焦提交 / Esc 取消 / Cmd·Ctrl+Enter 提交）—— 与 n8n「聚焦只对多行文本出现」同构。**顺带清账**：`steerNode` 与「重试带 overrides」**全链路删除**（重试只用节点自带参数），「能不能重试」统一走 `isReplayable` ⇒ music_generation / character_sheet / extract_last_frame 三类（**本就没有重放分支**）的 8 个节点不再显示重试，改「改完再点重生成」语义（链路真补齐见 CV-195）。**层叠收口**：nodes < ActionBar **8** < LayerPanel 10 < ReferenceTray 20 < **Drawer 30** < ContextMenu 50 < Modal 70。**验证**：新验收台 `scripts/preview-detail.mjs`（9 场景**单变量差分** —— A/B/C 只差工具条挂在哪儿、D↔A 只差 z-index 与 DOM 次序、E↔F 只差画布层 z-index、H↔I 只差 `bottomInset`，**对照场景必须红**；27 条断言 × 明暗两轨），实测真值：工具条 **187×31** 且 1× 与 0.4× **逐像素相同**、抽屉底边与容器差 **0.0px**、左右 20.0~740.0 与容器一致、两栏 280 / 400px、提示词宽 396px。**反向探针（改坏产品 CSS → 重新生成 → Chrome 实测 → 还原）逼出一条真缺陷**：`工具条压在节点之上` 原本**恒真** —— 工具条 DOM 次序本就在画布层之后，`z-index: auto` 也压得住 ⇒ 补一条「显式抬层 = z-index 必须是数字」的断言；三个变异复跑全部如期变红（z-index 改 auto / 抽屉退回固定 320px 宽 / 提示词 12→11px）。测试 **681/681 fail 0**、预览 **27 组全过**（25 → 27）。**批次 2 未做** = CV-195（三条重放链路）。 |
| CV-196 | **已修复·待验收** | P1 | **质检与预检体验优化**（用户反馈「校验太严 → 频繁重出图 + 红错刷屏」）：① `h3-ir-validate.ts` 新增 `prepareH3IrPrompt` **确定性修复层**（剥围栏/首尾空白/段序归一/段间空行/I2VA 对齐行重建与补齐/FL2VA·L2VA 时长重写——只动格式不动内容），两个视频工具接入，修复清单进 warnings；② **C1 表外运镜词 / T6 切镜动词 / D5 缺 [Language] 降 WARN**（标签不配对仍 ERROR）——红错只剩「发出去必浪费分钟级后端调用」的结构错误；③ `qc_shot` **auto 模式 skipped=true**（机器闸；confirm 保留轻量闭环——用户拍板）；④ skill：FAIL **修复式重跑**（drifts 转纠正指令，禁原样重跑；同漂移项重复不烧第二次预算）、WARN 不中断、**回合末「质检与预检汇总」**、**前置自检代替事后质检**（关键词出图前显式写全——用户拍板「后期质检让位前期关键词优化」）。测试 681 → **689 fail 0** | `src/h3-ir-validate.ts` / `src/host-tools.ts` / `src/quality-check.ts` / `skills-local/canvas-studio-creation/references/{consistency,toolchain}.md` / `skills-local/effect-test-runner/SKILL.md` / `tests/h3-ir-{validate,precheck}.test.mjs` / `tests/quality-check.test.mjs` / `docs/STATUS.md` |
| CV-201 | **已修复·待验收** | P1 | **内置 ffmpeg：装机即全功能**（用户提问「ffmpeg 好像不是内置的，别人电脑没装是不是就不能用？」——**是**）。**诊断**：依赖其实早就在（`canvas-studio` 的 `dependencies` 里有 `ffmpeg-static@5.3.0`），**二进制从来没进过包**：它靠 postinstall 从 GitHub Releases 下载，而 `.yarnrc.yml` 设了 `enableScripts:false`、根 `dependenciesMeta` 又没给 `ffmpeg-static:{built:true}` 豁免（electron / koffi / node-pty 都给了）⇒ `node_modules/ffmpeg-static/` 只有 `index.js`/`install.js`/`package.json`，**已打包的 app 里同样没有**；`src/ffmpeg-run.ts` 的 `require('ffmpeg-static')` 档因此恒被 `accessSync(X_OK)` 跳过 ⇒ **实际只剩系统 PATH 一条路**（开发机可用只因 brew 装了 ffmpeg）。**影响面**：成片合成 `compose.ts:408` / 抽帧 `video-style.ts:146` / 尾帧续镜 `video-frames.ts:107` **硬失败＝用户导不出成片**；BGM 真波形 `waveform-host.ts:96`、fal 参考图压缩 `providers/reference.ts:66`、产物时长与分辨率实测 `generate.ts:1579`/`:2177` 三处**静默降级**（看着能用，内容是假的）。**顺带审计**：桌面端 `src/` 所有 `spawn` 目标都是随包自带的（electron 本体 / 内置 node / 内置 pnpm / 安装器 / 用户 shell）⇒ **ffmpeg 是唯一缺口**。**硬约束**：用户拿到的是已打包 DMG/NSIS、**不跑 `yarn install`** ⇒ 只打开 `enableScripts` 救不了用户，二进制必须在**打包期**进 app；macOS 产物是 universal（x64+arm64）⇒ 两架构都得在包里且**不能落同一路径**（否则 `@electron/universal` 合并冲突）；`asar: true` ⇒ 可执行文件不能塞 asar（必须 `extraResources` 或 unpacked）；`hardenedRuntime` + `notarize` ⇒ 必须落在签名会覆盖的 `Contents/Resources/**` | `dsh-plugin-desktop/scripts/ffmpeg-bundle.ts`（新）/ `scripts/fetch-ffmpeg.ts`（新）/ `scripts/after-pack.ts`（新）/ `scripts/verify-licenses.mjs` / `dsh-plugin-desktop/package.json`（`extraResources` + `dist:*` 前置 + `afterPack`）/ `THIRD_PARTY_NOTICES.md` / `canvas-studio/src/ffmpeg-run.ts` / `tests/ffmpeg-bundled.test.mjs`（canvas-studio，新）/ `tests/ffmpeg-bundle.spec.ts`（desktop，新）/ `start-canvas-studio.sh` | ✅ 已落地（四目标 sha256 锁死 + 随包解析档 + afterPack 门禁 + 许可 + 文档；**完整方案与备选对比见 [plans/bundled-ffmpeg.md](./plans/bundled-ffmpeg.md)**）。**用户两条拍板**：mac 维持 universal 单包（原估 +42MB，**实测未压缩 118 MiB**）／许可先按现状 GPL 落地 —— ⚠️ **该拍板前提已被实测推翻**：产物 configure 含 `--enable-gpl --enable-version3 --enable-nonfree`，`--enable-nonfree` 触发 FFmpeg 随包 LICENSE 原文 *"unredistributable"* ⇒ **上游禁止再分发**，对外发布前须换 LGPL 构建（§7 B2 已升级为阻断项；内测/自用不受限）。**验收（K 组，必须在没装 ffmpeg 的干净机器上做）**：装 DMG → ① 成片合成 ② 尾帧续镜抽帧 ③ BGM 真波形 ④ 时长/分辨率实测 ⑤ fal 参考图压缩 ⑥ 缺组件时的用户向文案，全部见 [acceptance-test-cases.md](./acceptance-test-cases.md) §十一 K-1~K-7。**开发机须知**：`--dev` 会把宿主架构那份镜像进未打包的 Electron 资源目录，使 `process.resourcesPath` 在开发态与装机态解析到同一条路径 |

## P3 — 低优先级 / 远期记录

> 2026-09-02 竞品对标批量合并（[competitor-analysis.md](./competitor-analysis.md)）新增分区：价值真实但当前节奏下不进快速批次的条目。

| ID | 状态 | 优先级 | 问题 | 涉及文件 | 改进意见 |
| --- | --- | --- | --- | --- | --- |
| CV-075 | 暂缓 | P3 | 技能卡缺**作者署名/下载量**（竞品对标 DA-6）：竞品每卡有作者 + 认证标 + 下载量社交证明 | `skill-catalog.ts`（`author`/`installs`）、`SkillCard.tsx` | 若做：作者署名上「Canvas Studio 官方 ✓」（真实）；假下载量与「不伪造已生效」原则有张力，做前需拍板 |
| CV-076 | 已修复·待验收 | P3 | 技能卡无 **H3 能力角标**（竞品对标 DA-7）：竞品卡左上「H3」badge；我们 8/10 skill 实际基于 H3 能力，属**真实信息** | `skill-catalog.ts`（`h3?`）、`SkillCard.tsx`、`styles.ts` | ✅ 已落地：卡片左上角小 badge「H3」+ 详情弹窗同步展示，纯展示，成本低 |
| CV-077 | 已修复·待验收（过滤；排序未做） | P3 | 技能广场无「仅显示未激活」过滤与**排序**（竞品对标 DA-8）：竞品「其他Skill」区有过滤单选 + 排序下拉 | `SkillMarket.tsx` | ✅ 部分落地：「仅显示未装载」checkbox 过滤；**排序未实现**（skill 少价值低，如需后续补） |
| CV-078 | 已修复·待验收 | P3 | 技能广场无**创作者社区 CTA 收尾卡**（竞品对标 DA-9）：竞品网格末尾虚线框「加入创作者社区」投稿激励卡 | `SkillMarket.tsx`、`styles.ts` | ✅ 已落地：网格末尾虚线框 CTA 卡（纯展示 + `csReserved` 待接入角标），文案对齐 skill 扩展规范（docs/skill-expansion-spec.md） |
| CV-085 | 远期记录 | P3 | 视频 hover **快捷工具条**（竞品对标 DA-16）：竞品 hover 视频有剪辑/音频分离/存为资产等操作 | 前置依赖 CV-006（时间轴排除）/ CV-040（Master Audio）/ CV-043（音频参考）与素材库（仅设计） | 前置成片编辑能力全未落地；现有右键菜单已覆盖下载/引用/重试/查看详情。**待前置能力落地后再评估** |
| CV-086 | 已由 CV-091 实现 | P3 | 项目列表无**分组管理**（竞品对标 DA-17）：竞品左栏「项目 / 未分组」可折叠分组头；我们平铺 | `projects.ts`（`groupId`）、`ProjectList.tsx`、`routes.ts`、`client/*` | 原「建议缓做」；用户贴竞品截图讨论后拍板做，2026-09-03 由 CV-091 落地：用户自定义分组（独立 `groups.json`）+ 折叠头（折叠态存 localStorage）+ 顶部「+ 新建分组」+ 行内「移动到分组」下拉（非拖拽） |
| CV-091 | 已修复·待验收 | P3 | **左侧栏用户自定义分组 + 折叠**（竞品 DA-17 实际落地）：新建项目走分组头 `[+]` 就地展开名称输入（非全屏 modal），侧栏顶部「+ 新建分组」建组，项目行 hover 出「移动到分组」下拉归组（非拖拽），侧栏只分组可折叠、整栏不收起，折叠态按 `groupId` 存 localStorage 刷新保持，「未分组」桶常驻兜底（老项目 + 新建未分组） | `contracts/project.ts`、`projects.ts`、`routes.ts`、`client/api.ts`、`client/contracts.ts`、`client/project-store.ts`、`client/index.ts`、`client/ProjectList.tsx`、`styles.ts` | 用户自定义分组（需动数据层，加 `groupId`）；分组元信息独立 `groups.json`（不 bump `REGISTRY_VERSION`，缺失/损坏降级空数组，零迁移风险）。全链路：① 契约 `StudioProject.groupId?` + `StudioProjectGroup{id,name,order}`；② `ProjectRegistry` `create(name,groupId?)` 校验分组存在、`listGroups/createGroup/renameGroup/deleteGroup/moveProjectToGroup`（删组组内项目回落未分组）；③ `routes.ts` `ROUTE_GROUPS` GET/POST/PATCH/DELETE（复用 authority + same-origin 校验）；④ `client/api.ts` 5 个 fetch 封装；⑤ `client/contracts.ts` 5 个 inject 声明（StudioFrame props 唯一源头，漏了 TS2339）；⑥ `client/project-store.ts` `groups` state + `setGroups` 按 order 升序；⑦ `client/index.ts` 5 个回调（刷新经 `listStudioGroups`）；⑧ `ProjectList.tsx` 顶部双按钮 + 未分组桶 + 各分组 section（折叠头 `▾/▸`、双击重命名、hover `[+]` 组内新建、`×` 删组、空态）+ 行内 `csProjectMove` 下拉；⑨ `styles.ts` 补 `csProjectGroup*`/`csProjectMove`/`csProjectRowActions`/`csProjectListActions` 15 类。验证链全绿：typecheck（Host+Client）✓ / build（tsdown + tsc）✓ / test:smoke **195/195** ✓。**待桌面验收**：重启桌面新建分组 → 折叠/展开持久化、组内新建、移动到分组、双击改名、删组回落未分组 |
| CV-088 | 已修复·待验收 | P3 | Lobby 无**个性化问候**（竞品对标 DA-19）：竞品右栏「你好，{用户名} + 副标题」；LobbyHero 是固定品牌文案 | `LobbyHero.tsx`、`brand-copy.ts`（USER_MOCK 与 CV-069 同源） | ✅ 已落地：LobbyHero 品牌条增「你好，{USER_MOCK.name}，从一句话创意开始。」问候行，persona 与 CV-069 用户卡同源 |

## 决策点（实施前需对齐）

| ID | 状态 | 问题 | 候选方案 |
| --- | --- | --- | --- |
| D1 | 已拍板 | CV-001 文本编辑放哪 | **方案 A**：双击文本类节点=内联编辑、媒体类双击=详情（按 kind 区分语义）（2026-08-28 确认） |
| D2 | 已拍板（延后） | CV-007 时间轴定位 | 时间轴暂未想好，优先级后置——CV-006/007 延后，不进当前批次（2026-08-28 确认） |
| D3 | 已拍板（延后） | CV-005 连线删除语义 | 删除能力为「素材重新生成多版后择优」场景服务：用户可在多版素材中选择中意的一版（移除指向旧版的血缘）。与版本选择工作流合并设计后实施，暂缓（2026-08-28 确认） |

## 变更记录

| 日期 | 条目 | 变更 | 备注 |
| --- | --- | --- | --- |
| 2026-09-21 | CV-223（新增） | 立项并当日落地：「整理布局」升级**镜位泳道** —— 行 = 镜号（demo `canvas-layout-v2.html` 定稿方案移植进 `computeArrangeLayout`，签名不变）。行内泳道 分镜卡 → 场景图×k → 关键帧 → 视频/托盘 → 末帧；镜号四趟血缘传播（卡 title → 视频 → 末帧/关键帧 → 无血缘场景图取最小消费镜号，消费者 ≥8 = 全局锚留源区）；头部行（创意/上传/全局锚/源 BGM）+ 右上尾区（compose 恒末行）+ 兜底行保证任何画布不重叠；托盘按子代视频镜号进镜位行（组随行不变）；顶层被取代节点钉在取代链尾正下方且所在行加高。测试重写 8→13 条，canvas-arrange 3 条基线红翻绿（`tests 866 / pass 861 / fail 5`，余红同名基线）；罗大佑 83 节点真实画布冒烟：83/83 落位、重叠 0、18 卡 18 行、18 托盘同行对齐。后续候选：镜位框渲染视觉（CanvasSurface 画每镜圆角框 +「镜 N」chip）与引用束折叠（渲染层） | STATUS §4/§8 已登记 |
| 2026-08-28 | 全部 | 初版建单（21 项 + 3 决策点），来源：全量代码审查 | |
| 2026-08-28 | CV-012、CV-022（新增） | 完成生成参数结构化展示与血缘自动反查；新增 CV-023（创意节点，方案 A）、CV-024（落点策略）；health 负缓存测试对齐「失败不缓存」新语义 | 测试套件 74/74 全绿；顺带修复：`generate.ts` 运行时配置未注入时的编译期默认值兜底（`runtime()`）、`createStudioTools` cfg 可选化 |
| 2026-08-28 | CV-023 | 完成：`brief-capture.ts` 捕获首条真人消息 → `addBriefNode` 幂等落「创意」节点；index.ts 三处 reload 链上补落 + 暂存竞态处理 | 测试 74/74；旧项目打开时经会话历史重放自动补落 |
| 2026-08-28 | CV-024、CV-025（新增） | 完成：落点策略（血缘来源右侧 + 防重叠 + 网格回退）接入 generateAsset/splitStoryboard；创意→分镜/文案自动连边（BRIEF_NODE_TOOL 上移共享契约） | 测试 76/76（新增落点策略、创意血缘 2 个用例） |
| 2026-08-28 | CV-026（新增） | 完成：分镜表逐镜拆分为独立节点（parseStoryboardShots + formatStoryboardShot，血缘指向创意、每行 3 卡横向排列、解析失败回退单节点） | 测试 77/77 |
| 2026-08-28 | CV-027（新增） | 完成：shotRefs 参数（三种写法解析）让关键帧/视频连到所属分镜卡并右侧落位；submit 双模式拆卡并列出卡片清单；skill 教用法 | 测试 79/79（新增 shotRefs、放手跑拆卡 2 个用例） |
| 2026-08-28 | CV-028（新增） | 完成：生成节点画布框改为预览尺寸（previewSizeOf），媒体分辨率只入 mediaWidth/Height 与工具返回值 | 测试 79/79（落点策略用例补显示尺寸/分辨率断言） |
| 2026-08-28 | CV-029（新增） | 完成：媒体框比例自适应——长边固定 480、短边按真实比例缩放（用户修订规则）。上传图片落卡前探测尺寸直接按规则创建；媒体加载校正兜底（偏差 >5%、锁定跳过、不循环） | 测试 79/79；CanvasNode onLoad/onLoadedMetadata → CanvasSurface 透传 → StudioFrame updateNode + persist |
| 2026-08-28 | CV-013 | 完成：上传图片探测的真实分辨率直接写入 mediaWidth/mediaHeight；媒体加载回调（onMediaNatural）对存量缺失节点自动回填（锁定节点也回填分辨率、只不动框） | 测试 79/79；详情面板「分辨率」不再显示「未知」 |
| 2026-08-28 | CV-004、CV-003、CV-002 | 完成：标签共享模块 `client/canvas/labels.ts`（五处共用、补齐 storyboard-split）；Minimap 视口居中改用 CanvasSurface 容器实测尺寸（ResizeObserver）；ask_user_choice 自由输入框（allowFreeText）。决策点拍板：D1=方案 A、D2/D3 延后（时间轴定位待定；连线删除并入「多版素材择优」工作流设计） | 测试 79/79 |
| 2026-08-28 | CV-030（新增）、CV-009、CV-010、CV-001 | 新增 CV-030（双击开详情后单击其它节点也直开详情，用户验收发现，待处理）。完成：图层面板点击居中定位（focusNodeId）；loading overlay 已耗时 MM:SS + 超 3 分钟打断提示；文本类节点编辑（D1 方案 A：双击内联 textarea + 详情面板正文区，经 updateNode 持久化） | 测试 79/79 |
| 2026-08-28 | CV-030 | 完成：detailOpen 布尔改为 detailNodeId（渲染条件 selectedNode.id === detailNodeId），单击不再误开详情面板；关闭/删除/时间轴跳转清空，右键菜单按 id 打开 | 测试 79/79 |
| 2026-08-28 | CV-031（新增） | 完成：视频生成自动继承分镜卡血缘（inheritShotCardIds）——关键帧挂着分镜卡时，即使模型漏传 shotRefs，视频也同时连关键帧与分镜卡；只上溯一层只认分镜卡 | 测试 80/80（新增继承用例）；**未提交，等用户验证** |
| 2026-08-28 | CV-031（补充）、CV-014、CV-032（新增） | 用户以 VideoOut 真实 canvas.json 复核：存在第二种断裂模式（模型重上传关键帧拿新 filename，视频只连分镜卡漏关键帧）。补 `backfillUploadFilename`——upload_image 上传画布资产时回写新 filename 到节点。连线可见性：线宽/箭头/chip 按 1/scale 反向补偿（屏幕尺寸恒定），chip 低缩放（<0.6）隐藏、选中边保留 | 测试 89/89；**未提交，等用户验证** |
| 2026-08-28 | CV-033（新增） | 完成：删项目同步摘除绑定 workspace + 打开项目前清理同名孤儿 workspace，修复「删项目后重建同名报 workspace-name-conflict」 | 测试 89/89；**未提交，等用户验证** |
| 2026-08-28 | CV-034（新增） | 完成：画布映射优先用当前会话 cwd（session summary 自带），recentWorkspaceId 降为兜底；会话列表变化接入同步订阅。修复启动后「对话有内容、画布空、列表无选中」三不一致（孤儿 workspace 空会话把 recent 推导带偏） | 测试 89/89；**未提交，等用户验证** |
| 2026-08-28 | CV-035~038（新增，仅记录不实施） | 用户验收反馈四项：① 画布网格偏深（CV-035，P2）；② 项目缺「已完成」标记（CV-036，P1，方案 A/B 待拍板）；③ 节点右键菜单全部点击无效（CV-037，P0，高置信根因：window mousedown 关闭监听抢在 click 之前卸载菜单）；④ 手动连线起草线起点偏移且是直线（CV-038，P2，draft 应锚定节点右缘中点 + 贝塞尔） | 仅记录，未动代码 |
| 2026-08-28 | MiniMax Design / H3 开源仓库竞品调研 + CV-039~043（新增，仅记录不实施） | 对标 design.minimaxi.com 与 MiniMax-H3 开源仓库后确认：H3 原生联合生成音视频（对白/BGM/音效/画面文字全由提示词驱动），无需自建音频字幕生成服务。新增：CV-039（P1，skill H3 提示词规范升级为官方三字段结构）；CV-040（P1，Master Audio 全局音轨装配，compose.ts 已有 amix 半成品）；CV-041（P2，接入官方 h3-prompt-writing skill，agent-portable）；CV-042（P2，风格化画面文字/排版层）；CV-043（P2 远期，Ref2VA 音频参考与复用）。远期另记但不立条目：多版本批量派生、EDL 导出、3D 预演轻量替代（结构化运镜字段） | 定位共识：MiniMax 画布=生产流水线；我们的画布=Agent 创作过程驾驶舱（透明/可控/可追溯/开放底座），不拼产能拼差异 |
| 2026-08-29 | CV-037、CV-018、CV-035（「批次 0 实测减负」） | 完成三项：① CV-037（P0）右键菜单修复 —— `CanvasContextMenu` 改 forwardRef 挂 `menuRef`，mousedown 命中菜单内部时放行（判定抽为纯函数 `shouldKeepMenuOpen`），补 Escape 关闭；② CV-018 失败徽章兼作就地重试按钮（可见性判定 `canRetryNode` 与 `rerunNode` 前置检查一致），`onRetry` 经 CanvasSurface 透传；③ CV-035 网格线降到 45% 不透明度（color-mix 跟随主题，40px 格子不变）。新增 `src/canvas-actions.ts` + `tests/canvas-actions.test.mjs`（7 例） | 测试 100/100；typecheck（Host + Client）0 错；verify:loader 通过。**已提交 `6b3091a772`（未推送，沙箱无 push 凭据）；待桌面回归** |
| 2026-08-29 | CV-038、CV-020、HITL-C（「批次 1 快赢」） | 完成三项：① CV-038 起草线修复 —— 新增共享几何模块 `src/canvas-geometry.ts`（`edgeAnchor`/`edgePath`），draft 起点锚定来源节点右缘中点、路径与正式边同为 C 贝塞尔（指针居左时镜像控制点），正式边同步切换同模块消除漂移；② CV-020 资产下载 —— 右键菜单「下载资产」（不可下载置灰）+ 详情面板下载按钮，下载名清洗防目录穿越（`assetDownloadName`），纯函数可单测，DOM 触发留 client；③ HITL-C 设置页「默认执行模式」死开关处理 —— 核实后**未删**：`plan.md` 将 `workflowMode` 等字段统一定义为 ⏳ reserved 前向配置，改为全部 reserved 字段统一挂「待接入」角标（新增 `csReserved` 样式），与「不伪造已生效」原则对齐。新增 `tests/canvas-geometry.test.mjs`（10 例）+ 下载名清洗 3 例 | 测试 113/113；typecheck（Host + Client）0 错；verify:loader 通过。**已提交 `5eb97acb5b`（未推送）；待桌面回归** |
| 2026-08-29 | CV-011、CV-015、CV-016、CV-017、CV-019（「批次 2 交互补全」首批） | 完成五项：① CV-011 参考角标（左下「参考 · 角色」色点徽章，`data-role` 四色）+ 托盘空态引导卡片；② CV-015 toast 替换全部 7 处 `window.alert`（底部居中、3.5s/6s 自动消失、role=status）；③ CV-016 空白右键菜单（新建便签/文本/提示落光标处 + 粘贴 + 适配视野；`addNode` 加可选 `at` 落点）；④ CV-017 方向键微调（1px/Shift 10px，锁定跳过，800ms 连发一条 undo 快照，`computeNudge` 纯函数 +3 例）；⑤ CV-019 `zoomToSelection` handle + 双击空白 fit（fit 数学抽 `fitToBounds` 共用）。CV-008 多选拖拽/框选**未做**，需先与用户确认方案 | 测试 116/116；typecheck（Host + Client）0 错；verify:loader 通过。**未提交，待桌面回归** |
| 2026-08-31 | CV-044（新增） | 完成：双击视频改为固定尺寸播放浮层（新增 `client/canvas/VideoPlayerModal.tsx`，视频按真实分辨率渲染、上限 960px 宽 / 80vh 高、保持宽高比，Escape / backdrop / × 关闭，关闭即卸载停止播放）+ 视频元素挂原生 capture dblclick 监听拦截 shadow DOM「双击=全屏」+ CSS 隐藏原生全屏按钮（大屏播放统一走浮层）。图片双击仍为详情面板（D1 语义不变） | 测试 116/116；typecheck（Host + Client）0 错；verify:loader 通过。**待桌面回归** |
| 2026-08-31 | CV-044 修复（用户实测反馈「仍进全屏」） | 根因两层：① 拦截挂 `<video>` target 阶段无效——Chromium 内部监听注册更早，须在 video **父级 capture** 拦截；② 元素级 `stopPropagation` 截断冒泡致 React onDoubleClick 收不到 → 浮层打不开。修复：`CanvasNode` 拦截移到 `.csNodeMediaBox` capture + `preventDefault` 后经 ref 直调 `onOpenPlayback`；`VideoPlayerModal` 浮层内 video 同样包父级 capture 拦截（防浮层内再全屏） | 测试 116/116；typecheck 0 错；verify:loader 通过；lib 已重建。**待重启桌面验收** |
| 2026-08-31 | CV-045（新增） | 完成：① 图片双击弹大图预览浮层（新增 `ImagePreviewModal.tsx`，复用弹窗样式，图片 max-width 960px/80vh 保持比例，点关闭按钮/浮层外背景/Escape 关闭）；② 「查看详情」入口从「双击媒体节点」移入右键菜单（新增「查看详情」项）。双击语义更新：文本=内联编辑、视频=播放浮层、图片=大图预览、其余（group 等）=详情面板 | 测试 116/116；typecheck（Host + Client）0 错；verify:loader 通过；lib 已重建。**待重启桌面验收** |
| 2026-08-31 | CV-044 二次修复（用户反馈「先闪全屏再退出」） | 根因：Chromium 原生控件「双击=全屏」是内部直接调 `requestFullscreen()`，非可取消默认动作，`preventDefault` 拦不住。修复：在 video 实例上覆盖 `el.requestFullscreen` 使原生全屏调用 no-op（canvas 内 video + 浮层内 video 两处）。彻底屏蔽双击全屏 | 测试 116/116；typecheck 0 错；verify:loader 通过；lib 已重建。**待重启桌面验收** |
| 2026-08-31 | CV-044 三次修复（根因彻底定位） | 前两次方向皆错：原生控件「双击=全屏」是 shadow DOM 内部 C++ 路径触发，**不经过 JS requestFullscreen、也非可取消默认动作**，覆盖方法/事件拦截全无效。正解：**移除 `controls` 属性**（双击全屏 handler 随 controls 消失），双击正常冒泡走 React onDoubleClick。画布内 video 去 controls（缩略预览），浮层 VideoPlayerModal 去 controls 改「点击画面切换播放/暂停 + 居中 ▶ 图标」；清理 videoRef/mediaBoxRef 死代码 | 测试 116/116；typecheck 0 错；verify:loader 通过；lib 已重建。**待重启桌面验收** |
| 2026-09-01 | CV-046~048（新增，仅记录不实施） | 与用户讨论「本机已有 DSH（web 端/客户端）时本项目是否冲突」后立项：代码核实端口策略（43120 起重试 32 个）、DSH home 解析（显式配置 > `$DSH_HOME` > `~/.dsh`）、profile 机制与注册表落盘位置（`$DSH_HOME/canvas-studio/`，home 根下跨 profile 共享）。结论：多数情况可并存；核心风险是注册表「常驻内存 + 原子写」在双实例共享 home 时 last-writer-wins 互踩。新增 CV-046（P1，注册表迁 profile 级目录或加文件锁）、CV-047（P2，端口漂移提示）、CV-048（P2，并存官方指引文档） | 仅记录，未动代码 |
| 2026-09-01 | CV-049（新增，仅记录不实施） | 用户提出：找用户确认时卡片应常驻自由输入框，解决「选项里没有用户想要的内容」的情况。代码核实：自由输入目前是 opt-in（仅模型显式传 `allowFreeText=true` 才渲染，见 host-tools.ts L809 / question-capture.tsx L170），模型没传时用户没有自填出口。方案：契约语义反转为缺省 true、仅显式 false 隐藏（推荐），skill 同步移除「开放要素才传」的教导 | 仅记录，未动代码 |
| 2026-09-01 | 文档体系整理 + CV-050~055（新增，仅记录不实施） | ① 新建 [STATUS.md](./STATUS.md)（唯一事实来源，合并 CV/O/F/R 四套历史编号）、[README.md](./README.md)（文档索引）、[DEV-WORKFLOW.md](./DEV-WORKFLOW.md)（含「收尾必更文档」强制步骤）、[redo-redesign-plan.md](./redo-redesign-plan.md)（画布重做整改方案）；② 把根目录 `docs/canvas-studio-optimization-plan.md` 归位到本目录并标注「纯设计、代码零落地」；③ 补 CV-045 条目（此前只存在于变更记录、未进表）；④ **修正 CV-039/041 状态**——核实 `creation-spec.ts` 中三字段零匹配（next-steps-review.md 原判断有误），CV-041 转已完成、CV-039 维持待处理·部分；⑤ 新增 CV-050~055（分镜卡复用 / 关键帧打回 / 模式切换状态误翻 / 审批条醒目化 / 单版本回退 / 下游过时标记） | 仅文档，未动代码 |
| 2026-09-01 | **CV-052 + CV-056 修复（代码落地，待验收）** | ① setMode 状态决策抽为纯函数 `resolveSetModePatch`（`contracts/project.ts`）：mode 未变化短路只写 mode，绝不碰 state（CV-052 核心）；`routes.ts` 改为调用该函数并删除内联三条判据。② `StudioFrame.tsx` 模式按钮加 `disabled`（当前模式不可点）+ `styles.ts` 补 `:disabled` 样式（防御层）。③ `client/index.ts` `setWorkflowMode`：切换前快照 `before.state`，等待类（`awaiting_approval` / `keyframe_review`）→ `executing` 时复用 wakeAgent 发「继续」（CV-056）。④ 新增 `tests/workflow-mode.test.mjs` 7 用例（死锁格 / 真切换各格 / normalizeWorkflow 回归） | 验证链全绿：build ✓ / test:smoke **153/153** ✓ / typecheck（Host+Client）✓ / verify:loader ✓。**待桌面验收**：keyframe_review 点已激活模式 → 确认条不消失；awaiting_approval 切放手跑 → AI 收到「继续」自动续跑 |
| 2026-09-01 | **新增 CV-057~060（用户一批报 4 项）** | ① CV-057 视频播放浮层无进度条/无控制按钮——硬约束：原生 controls 因 CV-044（双击全屏 shadow DOM 路径）永不可用，方案=浮层自绘迷你控制条；② CV-058 上传图片无选项——已代码核实属实（handleUploadImage 直落 addImportNode、无任何中途选项），且 `StudioFrame.tsx:535` 引导文案「上传图片时勾选『设为参考图』」与实际交互不符（文案撒谎型缺陷），推荐先改文案再定是否加选项；③ CV-059 工具栏图标化改版（去设置按钮 + 三按钮移最右 + 全图标化）——前置待拍板：设置按钮是设置弹窗唯一入口；④ CV-060 应用名迁移半途（标题栏 `ExtendedTitlebar.tsx:181` 与 `index.ts:394` productName 仍 DSH Desktop，托盘/更新已叫 VideoBuddy）——统一为 VideoBuddy，需排查 recovery-copy/日志文案残留 + userData 目录迁移影响 | 均待处理。四项全部同步 STATUS.md §2.1/§4/§8 |
| 2026-09-01 | **CV-058 / CV-059 双拍板 + 上传链路核实** | ① CV-059 拍板：设置入口用 app 左下角全局入口，画布设置按钮直接删（待核对画布弹窗与全局设置是否同实例/同持久化）；② CV-058 拍板：只改文案不加选项——上传功能后续在 LLM 编排里已有；③ 上传链路核实（回应「上传到底传没传后台」）：**已双写**——`handleUploadImage`（StudioFrame.tsx:225）→ `/canvas-studio/upload` → `uploadLocalImage`（generate.ts:304）本地落盘 assets + `uploadBytesToDrama`（generate.ts:334）同步传 Drama `uploadimage` 拿 filename 落节点；发现副作用：Drama 不可用时整次上传失败、本地节点也不落（all-or-nothing），已记录暂不动 | 两行状态维持待处理，拍板结论写入条目 |
| 2026-09-01 | **CV-057~060 修复（代码落地，待验收）** | ① CV-057 `VideoPlayerModal.tsx` 自绘控制条：进度条 pointer-capture 可拖 + 当前/总时长 + 播放暂停 SVG 按钮 + 音量 slider + 静音切换，仍不挂原生 controls（CV-044 双击全屏硬约束），`styles.ts` 补 `csVideoControls` 等 7 类样式；② CV-058 `StudioFrame.tsx` 空态引导去掉「上传时勾选设为参考图」谎言，改指「上传后在详情面板点标记为参考」；③ CV-059 `CanvasToolbar.tsx` 设 `settings:false` 移除按钮（prop 保留接线）、整理布局/图层/小地图移最右图标组（内联 SVG 16px stroke 2，title/aria-label 保留，图层/小地图开关态高亮 `csToolbarIconActive`）；④ CV-060 `ExtendedTitlebar.tsx:182` span + `index.ts:398-399` productName/windowTitle 统一 VideoBuddy，并替换 tray-locale / native-dialog-copy / recovery-copy / desktop-dialog-window 共 53 处用户可见文案（main.ts PRODUCT_NAME 不动，避免 userData 迁移）。验证链全绿：build ✓ / test:smoke **154/154** ✓ / typecheck（canvas-studio Host+Client + dsh-plugin-desktop Host+Client）✓ / verify:loader ✓ | 四行状态 → **已修复·待验收**（待处理 22→18）。**残留**（原 CV-060 范围未含）：setup-wizard-copy.ts / desktop-settings-locales.ts / update-lifecycle.ts / native-ui/*.html / desktop-terminal.ts 仍有用户可见「DSH Desktop」，待立 CV-063 全量 rebrand 决策 |
| 2026-09-02 | **CV-064~066 立项（需求 1 + 需求 2）** | 需求 1 lobby 布局（无项目 chat 居中 / 有项目切回三栏）：A CSS class 切换 / B JSX 双形态 / C 混合 → 拍板 **C**。需求 2 技能广场：参照 MiniMaxHub（图 #2，独立全屏页 + 左分类侧栏 + 添加按钮）与 CodeL（图 #3，暗色 Modal + 使用按钮）；关键差异——**我们的 skill「激活」= 把 SKILL.md 注入 agent 上下文，不是装到工具栏** → 拍板 **F**（lobby 横滚推荐卡 + "更多"进全屏广场，激活后 work 态顶部 chip 常驻）。完整方案：[lobby-skill-marketplace-plan.md](./lobby-skill-marketplace-plan.md)（Phase A 布局 / B 数据层 / C UI / D 激活链路） | 均待处理 |
| 2026-09-02 | **CV-064 Phase A 落地（lobby 布局）** | **修正原方案 C 的一处错误**：plan §1.3 要求 lobby 态在 `LobbyPanel` 里重渲染 conversation slot —— 那会让上游 `@deepseek-ai/dsh-client-ui-conversation` 整棵卸载重建，草稿 / 滚动位置 / 会话绑定全丢。改为 **`.csChat` 常驻挂载，只改 CSS grid**。① `styles.ts`：`.csFrame` 加 `transition: grid-template-columns 300ms`（`prefers-reduced-motion` 关闭）；`[data-mode="lobby"]` 把第三列压到 0px（**保持 3 列才能插值**，列数变化会退化成瞬跳）+ `grid-template-rows: auto minmax(0,1fr)`，`.csProjects` 跨两行占第 1 列、`.csCanvas` 落 (1,2)、`.csChat` 显式 `grid-area: 2/2/3/3` 并居中成卡片（宽 `min(880px, 100%-48px)`、高 `min(560px,100%)`）；② lobby 态 `.csToolbar` / `.csWorkflowBar` 用 `display:none` 让位（**保持挂载**，work 态 DOM 与交互零变化）；③ 新增 `LobbyHero.tsx`（横向紧凑品牌条 + 双 CTA）——原整屏 `StudioEmptyState`（`.csWelcome` 是 `height:100%` 的 grid）会把聊天挤出视口，组件保留但 lobby 不再渲染；④ `brand-copy.ts` 加 `LOBBY_COPY`；⑤ 新增 `scripts/preview-lobby.mjs`：从 `styles.ts` 抽 `STUDIO_STYLES` + 最小令牌表 + 骨架 DOM 生成单文件预览 HTML（lobby/work 切换 + 亮暗切换），不开桌面即可肉眼验收。**踩坑**：给 grid 加 `grid-template-rows` 前必须确认所有浮层子元素是 `position:fixed`（已核实 `.csDetailPanel` / `.csContextMenu` / `.csToasts` / `.csOverlay` / 各 Modal 全是），否则会被 auto-placement 抓进显式行、破坏布局 | 验证链全绿：build ✓ / test:smoke **155/155** ✓ / typecheck（Host+Client）✓ / verify:loader ✓。**待桌面验收**（STATUS 状态 = 已修复·待验收） |
| 2026-09-02 | **CV-065 Phase B+C 落地（技能广场）** | Phase B 数据层：`src/skill-catalog.ts` 放 **src/ 根目录**（Host tsconfig 排除 src/client/**，根目录才能被单测直连 lib 产物）——`SKILL_CATALOG` 10 条真实元数据（name 与 skills/ 目录名逐字一致）+ 分类/图标/色相/featured 字段 + `getSkillEntry`/`skillsByCategory`/`skillCountByCategory`/`recommendedSkills`；`tests/skill-catalog.test.mjs` 5 例断言 skills/ 每个已注册 skill 都能在表取到条目（**新增 skill 忘补表直接红**）。Phase C UI：`SkillIcon.tsx`（6 种 inline SVG、currentColor + stroke 自适应主题、未收录 id 兜底「方块横线」不空白）、`SkillCard.tsx`（缩略图 hue 渐变现算 `hsl(h 70% 56%)` → `hsl(h+42 62% 42%)`，明暗主题自适应）、`SkillCarousel.tsx`（lobby 横滚，scrollBy ±420px 分页，**不做自动轮播**——抢焦点干扰输入）、`SkillMarket.tsx`（全屏覆盖层 `position:fixed inset:0 z-index:80` 盖住三栏而非替换容器，lobby/work 共用同一套进出逻辑；左分类侧栏 + 右卡片网格 `repeat(auto-fill, minmax(250px,1fr))`；Escape 关闭；「+ 新建技能」禁用 + `csReserved` 待接入角标）。接线：StudioFrame 挂 `csLobbyTail`（lobby grid 加**第三行 auto**、`.csProjects` 改跨三行 `1/1/4/2`）+ `skillMarketOpen` 条件渲染；CanvasToolbar props 加 `onOpenSkills` + 最右图标组加「技能广场」入口（work 态可进）；「使用」= `handleActivateSkill` 把提示词插进对话输入框（**复用 `insertReferenceToken` 通路**，不自动发送、不注入 system prompt——reserved 原则：不伪造已生效，也让 agent 自己决定是否 `skill(name=X)`）。**踩坑**：① 预览脚本里 `display:flex` 的元素 `hidden` 属性会失效（CSS 优先级覆盖 UA 样式），必须显式 `.csSkillMarket[hidden]{display:none}`；② mjs 里解构 `({SKILL_CATALOG: catalog, SKILL_CATEGORY_LABELS} = await import(...))` 赋值目标未声明会在严格模式抛 ReferenceError 被 catch 接住再抛，先 `let` 声明 | 验证链全绿：build ✓ / test:smoke **160/160** ✓ / typecheck（Host+Client）✓ / verify:loader ✓。**待桌面验收**（STATUS = 已修复·待验收）。CV-066 Phase D 见下条 |
| 2026-09-02 | **CV-066 Phase D 落地（skill 激活链路）** | 与方案 §4 的两处差异（见 STATUS.md CV-066 行）：① **持久化独立 `skills.json`**——`ProjectRegistry.readActiveSkills/writeActiveSkills`（原子写 + 去重 + 类型过滤，缺失/损坏 JSON 降级空数组），不碰 canvas.json 的 merge-protect 复杂逻辑；整表替换 API 幂等，activate/deactivate 都走 `saveActiveSkills`（单条增删不需要——列表最多 12 条，整表覆盖足够）；② **不做 send_system_message host 路由**——客户端已有 insertReferenceToken（插提示词）+ 对话发送自然唤醒 agent 两条现成通路。落地清单：`projects.ts` 增 `activeSkillsFile` + 读写 API（`tests/active-skills.test.mjs` 5 例：往返幂等 / 去重过滤 / 缺失降级 / 损坏降级 / 落盘格式）；`routes.ts` 增 `ROUTE_ACTIVE_SKILLS = '/canvas-studio/active-skills'`（GET/POST，复用 canvas 路由的 authority + same-origin 校验模式）+ `MAX_ACTIVE_SKILLS = 12`；`api.ts` 增 `loadActiveSkills/saveActiveSkills`；`project-store.ts` state 加 `activeSkills: Record<projectId, string[]>` + `setActiveSkills`（整表）/`activateSkill`（去重追加）/`deactivateSkill`（过滤移除）+ `activeSkillsOf` selector，`clearProject` 同步清空（删项目不留孤儿 key）；`ActiveSkillChips.tsx` 新组件（work 态工作流条下方，标题取 catalog、未收录显示注册名不空白，每 chip × 卸载）；`contracts.ts` `StudioProjectListInjected` 补 `activateSkill/deactivateSkill` 声明——**这是 StudioFrame props 类型的唯一源头，漏了就是 TS2339**；`client/index.ts` 接线（store 即时更新 + 落盘，失败回滚 store；`openProject` 载入 skills.json，失败静默下次重试）；`StudioFrame.tsx` work 态渲染 chip 行 + `handleActivateSkill` 升级（work 态同时装载）。**typecheck 修复记录**：`activateSkill`/`deactivateSkill` 从 props 解构报 TS2339（契约没补）+ 两处 `.catch((cause))` 报 TS7006——后者是前者的连带（`any` 调用回调失去上下文类型），契约修好后自动消失，与文件里既有 150+ 处 `.catch((cause))` 写法一致，不需要逐个加注解 | 验证链全绿：build ✓ / test:smoke **165/165** ✓（+5 例）/ typecheck（Host+Client）✓ / verify:loader ✓。**待桌面验收**（STATUS = 已修复·待验收）。验收法：lobby「使用」→ 仅插提示词无 chip；work 态「使用」→ 提示词进输入框 + 工作流条下方出现 chip；× 卸载 → chip 消失；重启桌面重开项目 → chip 仍在（skills.json 持久化） |
| 2026-09-02 | **CV-067 修复（竖屏成片被横屏框裁切）** | 用户实测反馈（附截图）：成片 9:16（480×864）在画布上横屏显示被 cover 裁切。根因定位：`compose_video` 落盘硬编码 `COMPOSED_SIZE 260×180`（横屏占位），真实分辨率只写 mediaWidth/mediaHeight；客户端 onMediaNatural 异步校正（依赖 video onLoadedMetadata 时序）兜底不稳。修复（方案 B = 收敛共享函数）：新增 `src/canvas-aspect.ts`（previewSizeOf 抽根级共享：长边 480 / 1:1→420×420 / 短边 60 地板），compose 落盘按真实分辨率换算，generate 删本地 previewSizeOf 改 import，StudioFrame 删 longSide480（上传探测 + 媒体加载校正统一走共享），消除三处规则漂移（1:1 曾 420/480 不一致）；tsconfig.client.json include 追加。新增 `tests/canvas-aspect.test.mjs` 7 例（16:9 / 9:16 / 1:1 / 480×864 主场景 / 极端比例地板） | 验证链全绿：build ✓ / test:smoke **172/172** ✓（+7）/ typecheck（Host+Client）✓ / verify:loader ✓。**待桌面验收**（STATUS = 已修复·待验收）。验收法：重跑 9:16 成片合成 → 节点框竖屏（≈267×480）无裁切；1:1 / 16:9 边界回看 |
| 2026-09-02 | **CV-068 视觉禁令护栏（agent 直读 file_path 报 does not declare image input）** | 用户实测截图反馈：生成「视觉预览」等中间产物后 agent 试图直接读本地图片自查 → 3 连错。**根因排查链**：① canvas-studio 工具只收 Drama 服务器 filename（image2vl 唯一看图通道，host-tools.ts:615）；工具结果回传是纯文本（renderResult，不产生 image block）→ 排除本项目工具层出错；② 错误串「does not declare image input / switch to an image-capable model」全仓源码搜索为空 → 报错在 DSH 运行时（桌面 bundle）——运行时把本地 png admit 成 image block 提交给 qwen3.8-27b（无 image input 声明）→ 能力校验直接抛错不降级；③ SKILL.md 旧护栏（「不要用文件读取工具打开图片」）太轻未拦住 agent 走捷径。修复（方案 A = 提示词护栏）：SKILL.md 核心规则升级显式禁令（无视觉能力声明 + 禁止 file_path/URL/内嵌/附件全部读图变体 + 产物 url 用途澄清 + image2vl 唯一合规通道与 upload_image 前置步骤），skills-local 源与 skills/ 产物双写一致（运行时扫 skills/ 注册，sync 脚本从 skills-local 拷贝，改单边会漂移）；host-tools.ts image2vl description 补工具级护栏（agent 调 image2vl 时可见兜底）。新增 `tests/skill-guardrail.test.mjs` 3 例（双副本一致 / 禁令关键词在位 / lib 产物含护栏） | 验证链全绿：build ✓ / test:smoke **175/175** ✓（+3）/ typecheck（Host+Client）✓ / verify:loader ✓。**待桌面验收**（STATUS = 已修复·待验收）。验收法：让 agent 生成 3 张预览图后说「检查一下效果」——应改为走 upload_image → image2vl（或文字汇报），不再出现 does not declare image input 报错 |
| 2026-09-02 | CV-069（新增，仅记录不实施） | 用户上传竞品截图（MiniMax Design 项目库 + 左栏底部用户信息面板），提出补用户信息功能（假数据 mock 可行，纯前端改动不碰 Host/契约）。拍板四项：① 主题/设置接真实功能（ctx.theme / 现有 SettingsModal——用户卡恰是 CV-059「设置入口 = 左下角」的插件内落点）；② 未规划条目（记忆管理/教程/更新日志/接入飞书微信）保留入口挂待接入，尽量完整复刻竞品结构；③ 用户卡三态常驻（lobby / lobby-pending / work）；④ 假数据用中文创作人设。方案与技术细节见 CV-069 条目 | 仅文档，未动代码 |
| 2026-09-02 | **CV-070~088 批量合并（竞品对标 19 条，[competitor-analysis.md](./competitor-analysis.md) → 正式编号）** | 三章草案正式合并：**P1 ×4** —— CV-070（技能卡动态预览，素材已就绪：style-demos 8 GIF 与 skill 同名对应）、CV-072（广场搜索）、CV-079（生成产物自动编组，**前置 CV-008 必须先修**）、CV-080（节点命名精准化）；**P2 ×8** —— CV-071（悬停浮层+详情）、CV-073（我的 Skill）、CV-074（精选分区，featured 字段已有）、CV-081（文本选中滚动）、CV-082（视频 hover 播放）、CV-083（时长角标，内联控件明确不做）、CV-084（制作计划阶段清单，单独排期）、CV-087（项目封面缩略图，列表性能红线）；**P3 ×7**（新增「P3 — 低优先级 / 远期记录」分区）—— CV-075（作者/下载量，**暂缓**用户拍板）、CV-076（H3 角标）、CV-077（过滤/排序）、CV-078（社区 CTA 卡）、CV-085（hover 工具条，**远期**）、CV-086（项目分组，建议缓做）、CV-088（Lobby 个性化问候，与 CV-069 同批） | 仅文档，未动代码；DA→CV 映射与完整方案见 competitor-analysis.md |
| 2026-09-02 | **竞品对标批次落地（16 条，待桌面验收）** | 按建议顺序五批全做：① CV-080 命名（`mediaNodeTitle`/`promptSummary` 纯函数 + 单镜命名）+ CV-081 文本滚动（选中态 auto + wheel 按可滚豁免）；② CV-082 hover 播放（150ms 延迟/单实例/降级/卸载清理）+ CV-083 时长角标（`formatMediaDuration` 现算，实现决策**不落盘**）；③ 技能广场 CV-070/072/074/071/073/076/077(过滤)/078（SkillMarket 改版：搜索、精选/其他分区、我的 Skill 视图、详情弹窗、H3 角标、仅显示未装载、社区 CTA 卡；SkillCard hover GIF 懒加载）；④ CV-008 多选整体拖拽（gesture origins + snap 均摊）+ marquee 框选（先清空再逐个加入防翻转 bug）+ CV-079 自动编组（`attachShotGroup`，generateAsset 改 writeCanvas 整表）；⑤ CV-069 用户卡（`UserCard.tsx` + USER_MOCK）+ CV-088 Lobby 问候。新增测试：mediaNodeTitle / attachShotGroup / formatMediaDuration / demo GIF 守卫；CV-031 与 shotRefs 测试适配整表写盘。**未动**：CV-075/084/085/086/087 | 验证链全绿：tsc Host emit ✓ / typecheck（Host+Client）✓ / tsdown ✓ / test:smoke **179/179** ✓ / verify:loader ✓ |
| 2026-09-02 | **CV-064 二期三态布局（lobby / lobby-pending / work）** | 用户拍板语义：**「新建项目后、未开始对话」= lobby 视觉但无右上角新建 CTA**（CTA 只在无项目时显示）；「输入创意并确认」→ work（图 1，含对话内容）。**判据**（先侦察上游类型再落地）：`sessionSvc.list` 当前会话 `blank` 字段——`blank=true` 无对话；首条 prompt ACCEPTED 后上游 manager 自动镜像 `blank→false` 进 list row，且 `sessionSvc.list.subscribe`（index.ts:521 已有）会触发 → **「点发送后立即切 work、不等 agent 响应」成立**（用户视角最自然，拍板方案）。落地：① `project-store.ts` state 增 `hasConversation: Record<projectId, boolean>`（**内存态不持久化**——恢复时 openProject 现算，不新增磁盘字段）+ `setHasConversation` action + `hasConversationOf` selector（`clearProject` 清空）；② `client/index.ts` 新增 `syncHasConversation()`（`resolveActiveProjectId` → 当前会话 blank 现算判据，幂等）挂三处：effect 主体、workspaces subscribe、sessions subscribe（blank 翻转自动切 work）、openProject 会话绑定后；③ `StudioFrame.tsx`：`mode` 三态（`projectId===null→'lobby' / hasConversation→'work' / 其余→'lobby-pending'`）、`canvasBody` 三分支（A→LobbyHero / B→`return null` 不渲染中栏 / C→画布+时间轴）、`csLobbyTail` 条件 `mode!=='work'`（A+B 都显示横滚）、chips 条件 `mode==='work'`（B 态隐藏——图 1 效果只在有对话后出现）+ `projectId!==null` 显式收窄（`mode==='work'` 无法让 TS 推导 projectId 非 null，TS2345）；④ `styles.ts` 全部 `[data-mode="lobby"]` 选择器扩成 `[data-mode="lobby"], [data-mode="lobby-pending"]` 双写（B 态复用 lobby 布局：第三列 0px + 中栏两行 + 聊天居中卡片），**不能 replace_all**（`.csFrame[data-mode="lobby"] .csProjects` 会被错误拆成两条独立选择器，第一条匹配 csFrame 自身）；⑤ 预览脚本升级三态循环演示（对话空态块 `pvMsgEmpty` + 项目行容器 `pvProjectRows`，`display:flex/grid` 元素 [hidden] 需显式补回）。**踩坑（重要）**：① **同一消息多个 Edit 并发改同一文件 → 各基于旧快照、写盘互相覆盖，只留最后一个**（project-store 6 连发、index 3 连发都只生效 1 个），必须一次一个串行；② macOS BSD grep 不认 `\|` 交替（返回空被误报「编辑未生效」），用 `grep -e A -e B` | 验证链全绿：build ✓ / test:smoke **165/165** ✓ / typecheck（Host+Client）✓ / verify:loader ✓。**待桌面验收**（STATUS = 已修复·待验收）。验收法：① 新建项目 → 聊天居中、无右上角新建 CTA、推荐技能横滚仍在；② 输入创意点发送 → 立即切 work（画布空态 + 用户气泡出现在右栏）；③ 重启桌面重开「无对话项目」→ 回 lobby-pending；重开「有对话项目」→ 直接 work |
| 2026-09-03 | **CV-089 画布选中/拖动视觉修正（代码落地，待验收）** + **CV-090 框选改进立项** | ① **dim 语义接反（本轮核心）**——用户「点选单张图拖动，其余图片全被蒙一层」。根因：`.csCanvasSurface[data-dragging="true"]` 把非被拖节点压到 opacity 0.55 / 0.85，而 `data-dragging` 是 `onNodePointerDown`（**节点拖动手势**）置上的；dim 的合理语义是「框选时区分命中/未命中」，两者接反。修复 = **删除 dim 规则**，拖动时不再改任何节点不透明度，只给主拖节点 `csNodePrimary`（z-index:3 + 加粗描边）；`draggingNodeIds` 数组 state 收敛为 `primaryDragId: string \| null`，并修掉「`primary` 从 `gesture.current`（ref）读 → 不触发 re-render → 永远拿到旧值」的隐性 bug。② **双击节点无反应**（视频+图片同根因）：pointerdown 立即 `setPointerCapture` 致 `mousedown/mouseup` 被 retarget 到容器，dblclick target 变成容器、节点 `onDoubleClick` 收不到（容器自己的双击 fit 一直正常 = 「双击空白有反应、双击节点全挂」）。改为 `armPointer` → 首次 move 才 `ensureCaptured()` + `DRAG_THRESHOLD = 3` 阈值，顺带消灭「双击手抖 1px → 位移 + 空 undo + 全量写盘」。③ 元信息条 `{dims !== null && ...}` 条件渲染 → 未就绪整行消失，改**始终渲染**（`— × —` / `加载中…`）。④ 选中态 `--dsw-alias-interactive-bg-active`（带透明度）→ `--cs-accent` 实色描边 + 外光晕。⑤ 分辨率角标；`.csNodeLinkHandle` 默认隐藏改 hover/选中才显；删 `csNodeRing` 死元素。**编号纠正**：这批改动曾在代码注释里误用 CV-072/CV-073（已归技能广场），已按行号精确改回 CV-089（23 处）。**CV-090 立项待处理**：框选无命中预览高亮 + 拖出画布即取消，拍板后再动 | 验证链全绿：typecheck（Host+Client）✓ / tsdown ✓（client.js 480KB）/ test:smoke **195/195** ✓。**踩坑复现**：CSS-in-JS 模板字符串注释里写带反引号的 CSS 选择器会被 TS 当模板字符串起始符 → TS1005 整文件解析失败（已删反引号） |
| 2026-09-03 | CV-091（新增，代码落地，待验收） | **左侧栏用户自定义分组 + 折叠**（竞品 DA-17 实际落地，原 CV-086 缓做项，用户贴竞品截图讨论后拍板做）：分组维度=用户自定义分组（`StudioProject.groupId?` + 独立 `groups.json`，`GROUPS_VERSION=1`，缺失/损坏降级空数组，零迁移风险）；顶部「+ 新建分组」建组，分组头 `[+]` 就地展开名称输入建项目（非全屏 modal），项目行 hover 出「移动到分组」下拉归组（非拖拽），分组可折叠、折叠态按 `groupId` 存 localStorage 刷新保持，「未分组」桶常驻兜底。全链路：契约 `project.ts`（`StudioProjectGroup{id,name,order}`）→ `ProjectRegistry`（`listGroups/createGroup/renameGroup/deleteGroup/moveProjectToGroup`）→ `routes.ts`（`ROUTE_GROUPS`）→ `client/api.ts`（5 fetch）→ `client/contracts.ts`（5 inject 声明）→ `client/project-store.ts`（`groups` state）→ `client/index.ts`（5 回调）→ `client/ProjectList.tsx`（重写分组渲染 + 折叠 + 行内 `csProjectMove` 下拉）+ `styles.ts`（补 15 类 `csProjectGroup*`/`csProjectMove`/`csProjectRowActions`/`csProjectListActions`）。顺带修既有缺陷：`index.ts` 漏导 `postStudioWorkflowAction` 致 workflow 审批（approve/confirm_keyframes/setMode）运行时 ReferenceError，补导入修复。验证链全绿：typecheck（Host+Client）✓ / build（tsdown + tsc）✓ / test:smoke **195/195** ✓ | 状态 → **已修复·待验收**（原 CV-086 转「已由 CV-091 实现」）。**待桌面验收**：重启桌面新建分组 → 折叠/展开持久化、组内新建、移动到分组、双击改名、删组回落未分组 |
| 2026-09-07 | **CV-101 东方异境视觉导演 skill 外挂试跑（第三方 MJ 向 skill）** | 原文 1720 行一字未改存入 `references/`，薄 `SKILL.md` 只做执行层映射：覆盖失效的 MJ 层（改 image_generate prompt + negativePrompt + H3 模式）、画幅 21:9 → 16:9/9:16/1:1、style 固定 realistic 禁 anime、三方向改走 ask_user_choice；catalog `hidden=true` 试跑期不上广场 | **待验收**：手动 `skill(name="oriental-mythic-visual-director")` 跑原文 §31「桂花·月下桂海」→ 看是否出到「真实电影人物 + 绘画化东方异境」；成立再升为第 9 个风格预设（接总纲风格表 + 大类追问） |
| 2026-09-15 | **CV-184 新节点落点收口 + 生成后带进视野（代码落地，待验收）** | 用户问「新生成的节点怎么放到画布上 / 整理布局点下去怎么工作」，**讲解时当场量出两个真问题**：① **落点规则散在 4 处**（`generate.ts` / `compose.ts` / `client/index.ts` / `client/project-store.ts` 的 `LAYOUT`），而**只有第一处带冲突避让** —— 无血缘分支一律盲取 `index`，第 20 张图固定落在已占的格子上（用户看到的「成片压在别人身上 / 一堆图叠成一摞」）。**收口**：新建 `src/canvas-placement.ts`（`deriveNodePlacement` 两分支**共用同一份 `clashes`**、无血缘改「扫首个不重叠格」、`placeSequence` 管批量推进、`boxesOverlap` 半开区间），四处调用改 import、`LAYOUT` 常量删除 —— 本质是对 CV-024「落点策略」的**单一实现化**（原「无来源回退网格空位」里的盲排就是 CV-024 留下的缺口）。② **生成完看不见生成物**：`canvas-view.ts` 新增纯函数 `revealOffsetOf`（**只算平移、永不改 scale**；框大于视口且已相交则不动）+ `CanvasSurface.revealNodes`（**手势进行中直接返回**）+ `StudioFrame` 的 `seenNodeIdsRef` **集合记忆**（只认首次出现、非 pending、非 brief）。**搁置项**：C1 镜次×阶段矩阵（实测 fit-scale 0.201 → **0.179 更差**，且 `StudioCanvasNode` 无 `shotIndex` 字段、要动数据契约）、B1 列内自适应（单独做 **0.201 → 0.201 无改善**，瓶颈在高度）→ 与 B2（包围盒贴近视口）/ B3（不再无条件缩放）一起排第二批 | 测试 645 → **654/654 fail 0**（+9，`tests/canvas-placement.test.mjs`）；预览台 **21 组全过**；反向验证 **11** 探针各自能红。**踩坑**：同消息对同一文件连发多条 Edit 会**互相覆盖**（`compose.ts` 里 `const placement = ...` 被后一条冲掉 → tsc TS2304），改一条一条串行发 |
| 2026-09-15 | **CV-185 「整理布局」按视口整形 + 适配视野的可读下限（代码落地，待验收）** | 用户第二轮问题（「整理布局点下去怎么工作、有没有优化空间」）的落地。**改前**：一个深度一条列、列内不限行、单元格取全局最大宽 → 真实画布 `测试音乐`（23 个单元全在 depth 0）排出 **768×6452 细长条**、占用 35%、适配比例撞 **0.1** 下限；`验收新建项目` 的盒子被一张 996 宽的托盘撑出 1248px 纯空白。**改后**：① 列宽按列自适应（**行高仍统一**，保住横向对齐）；② 行数上限 R 由「预测适配比例最大」搜出来，超了在**同深度内**往右开相邻子列（深度顺序严格不破）；R 下界 3 —— 放开到 1 会把同层节点摊成一排（列失去意义）且实测不改善；③ 适配数学从 `CanvasSurface` 的 JSX 收口成纯函数 `computeFitView`，加 `FIT_MIN_SCALE = 0.3` 下限，装不下时**对齐内容左上角**并从画布回调出去提示一次。实测适配比例 0.100→**0.433** / 0.322→**0.415** / 0.184→0.263（后两者屏上都被下限抬到 0.300，但新版盒高 1680 vs 3160、纵向不再溢出）；占用 35/29/31% → **60/52/53%** | 测试 654 → **663/663 fail 0**（+9，`tests/canvas-arrange.test.mjs`）；新增预览验收台 `scripts/preview-arrange.mjs`（真实实现摆两侧、按屏幕矩形断言，明暗 16 条）→ 预览台 **23 组全过**；反向验证 **13 探针各自能红**，预览组把「整理后」换成「整理前」**8 条红**。**三条被自己抓住的错**：用例拿「另一个成员的 delta」当期望值（恒真）、用例的样例让「搜索」与「固定下限」不可区分、预览台拿包围盒面积比大小（撞下限那侧反而更大） |
| 2026-09-15 | **CV-186 血缘压暗恢复（语义重做 + 绑手势）** | 用户要求把 2026-09-13 退场的「拖动时压暗远亲」加回来，并问「能否恢复 / 有没有更好的方案」。**先复盘退场真因**（读 CV-162 / CV-170 / CV-171 全套）：① **不是实现坏了，是判据坏了** —— `active = lit.size > selected.size` 在真实成链画布上**几乎恒为真**，实测 18 节点的「凌晨三点的门外人」选中任意节点都会压暗 9~16 个（连零血缘的「创意」也压暗 11 个：它的 6 个下游被点亮 ⇒ `7 > 1`）；② **压暗挂在选区上**，而选区全仓十几处写入点，只要一处漏清就「松手不恢复」；③ 原口径「1 跳亮 / 其余压暗」在真画布上平均压暗 **68%**、53 个拖动目标里 **39 个 >70%** —— 用户读到「一点就整屏变灰」。**恢复方案（用户四问全按推荐拍板）**：**仅拖动时生效**（`spotDragIds` 手势态，写入点是 `onPointerMove` 的**首次真实位移** → 单击/按住不闪灰）；**距离三档 A1**（1 跳亮 / 2 跳 `--cs-dim-near` 0.75 / ≥3 跳与无关 `--cs-dim` 0.42）；**两道安全阀**（① 被拖簇之外一个 1 跳邻居都没有 → 整体不启用，挡住「拖刚导入的孤立素材」「拖对外无血缘的托盘」；② 中间档覆盖 > 70% → 整体退亮档，因为星形项目里 22 张卡有 19 张落中间档、梯度没有区分度）；**绑手势不绑选区**（出口只有 `pointerup`/`pointercancel`/`pointerleave` 三条，结构上不可能「松手不恢复」）。**新增的一条契约**：托盘与成员**必须同档**（托盘自身无血缘，按自己距离判档会把装着来源图的托盘整体压暗）—— 取簇内最亮档，实测不加这条时「托盘与成员落进不同档」出现 **22 次**。**实测三口径对照**：A0 平均压暗 68%/最差 91%/39 个目标 >70% → **A1 28%/65%/0 个** → A2（只压无关）12%/20%/0 个。**验证**：`tests/canvas-lineage.test.mjs` 重写 13 用例（含两道安全阀各自的正反用例）、`tests/visual-tokens.test.mjs` 那条反向守卫**翻正**（旧的「不得出现 `dimmed=`」改「必须调 `canvasSpotlight(` 且必须真的把 tier 传下去」+「不得回到 `dimmed=` 旧形态」）、新增预览验收台 `scripts/preview-spotlight.mjs`（真实 `canvasSpotlight` 摆台 + **页内独立 `ORACLE` 期望表**，20 条 computed-opacity 断言明暗双轨）| 测试 663 → **669/669 fail 0**（+6）；预览台 **25 组全过**（新增「血缘明度」1 组 × 明暗）；真鼠标探针 `probe:surface` 更正断言 6c（CV-174 已改「松手塌缩为单选」）后 **34/34**；反向验证 **12** 探针各自能红（含「托盘取亮档初值写 1」「档位类名改坏」「投影强制恒亮」）。**探针逮到的真 bug**：托盘取最亮档的初值写成 `1` 时 `value < brightest` 永不成立、整簇被无条件抬亮，**而当时所有断言照样全绿** |
| 2026-09-17 | **CV-194 节点详情重构 · 批次 1（已完成，桌面验收通过）** | 用户三条吐槽（弹框在最右缘离节点远 / 详情难读 / 提示词只有单行输入）读码后确认同源：`.csDetailPanel` 是 `position: fixed; top: 64px; right: 12px; width: 320px` 的**屏幕坐标浮窗**。**改为「就近工具条 + 底部可拖高抽屉」**（用户拍板 B 方案，动手前先调研 React Flow NodeToolbar / n8n NDV / NodeTool / Grida 四家做法）。工具条渲染在 `.csCanvasLayer` **之外**（尺寸天然不随缩放变形）、位置由纯函数 `nodeActionAnchor` 现算；抽屉挂 `.csCanvasBody` 内绝对定位（底边＝时间轴顶边、宽度＝画布宽）。新增 `src/node-params.ts`（生成参数解析唯一源）+ `NodeActionBar.tsx` / `PromptEditor.tsx`（三档）/ `NodeDetailDrawer.tsx`，**删除 `LayerDetailPanel.tsx`（431 行）**；`steerNode` 与重试 overrides 全链路删除，判据统一走 `isReplayable` ⇒ 8 个不可重放节点不再显示重试。**反向探针逼出真缺陷**：`工具条压在节点之上` 原本恒真（DOM 次序本就靠后，`z-index:auto` 也压得住）⇒ 补「显式抬层 = z-index 必须是数字」断言。**遗留**：三条重放链路 = CV-195。 | tsdown + tsc×2 + verify:loader 全过；测试 **681/681 fail 0**；预览台 25 → **27 组全过**（新 `scripts/preview-detail.mjs`：9 场景单变量差分、27 断言 × 明暗两轨，含页内三组反向对照） |
| 2026-09-17 | **CV-201 内置 ffmpeg 落地（代码 + 门禁 + 文档，待验收）** | 用户问「ffmpeg 不是内置的吧？别人电脑没装是不是就用不了」——审计确认**是**，且坏的不是「一些操作」而是**导出成片**。**根因三层各自都对、串起来必断**：声明在（`ffmpeg-static@5.3.0`）/ 获取断（postinstall 被 `enableScripts:false` 跳过，根 `dependenciesMeta` 又没豁免 ⇒ 包内只有包壳）/ 解析断（`require('ffmpeg-static')` 档因文件不存在被 `accessSync(X_OK)` 跳过 ⇒ 只剩 PATH）。**方案 A（打包期取 + extraResources）落地六件**：① `scripts/ffmpeg-bundle.ts` —— 四目标（darwin-arm64/x64、win32-x64、linux-x64）**archive 与解压产物双 sha256 锁死**、体积校验、**容器架构嗅探**（Mach-O cputype / PE machine / ELF e_machine，不依赖 Rosetta）、幂等缓存（命中即离线复用）；② `scripts/fetch-ffmpeg.ts` —— `--mac/--win/--host/--all` + `--dev`（镜像进未打包 Electron 资源目录，使开发态与装机态走 `process.resourcesPath` 同一条路），`dist:mac|mac-smoke|win|win-portable` 与 `package:dir` **全部前置调用**（缺了直接红）；③ `extraResources` → `Contents/Resources/ffmpeg/<平台>-<架构>/`（两架构**分路径**绕开 universal 合并冲突；不在 asar 内 ⇒ 可 spawn；在 bundle 内 ⇒ 被签名公证覆盖）；④ `src/ffmpeg-run.ts` 新增**随包档**（`resourcesPath` + 模块祖先目录两路；纯 Node 下 `undefined` 自动跳过 ⇒ 不引入 electron 依赖），新顺序＝显式 → `FFMPEG_PATH` → **随包** → ffmpeg-static → PATH；错误文案改面向终端用户（删「brew install」）；⑤ `scripts/after-pack.ts`（新）合成 afterPack：运行时闭包 + **随包 ffmpeg 门禁**（缺文件/体积/哈希/架构/LICENSE 全红，且**真跑 `-version` 判耗时**，<5ms = 空绿直接红）；⑥ 许可：随包带上游 `LICENSE`，notices 与生成器同步加「Bundled executables」段。**验证**：canvas-studio build + typecheck + verify:loader + 全量 **768/768 fail 0**（新增 `tests/ffmpeg-bundled.test.mjs` 5 例，含「随包优先于 PATH」与「文案不得出现 brew install」）；desktop `tests/ffmpeg-bundle.spec.ts` **24/24**（含「瞬间返回的 `-version` 必须红」这条反向守卫）+ `package.spec.ts` 补 afterPack/extraResources 断言；`fetch-ffmpeg.ts --mac` 实跑：下载 44MB → 校验 → 落盘 → `ffmpeg version 6.0` 真跑 **704ms**（二次运行缓存命中，14ms） | 详见 [plans/bundled-ffmpeg.md](./plans/bundled-ffmpeg.md)（含 A~E 五方案对比与七条风险表）。**包体** mac universal +42MB；**许可** GPL-3.0-or-later 先落地（⚠️ 实测含 `--enable-nonfree`，法务复核） |
| 2026-09-17 | **CV-195 / CV-050 / CV-051 / CV-046 落地（代码 + 用例，待验收）** | 功能补齐收尾批：三条离线产物（BGM / 四视图 / 抽帧）补上原地重放链路（`overwriteNodeAsset` + `replayDetachedAsset`，四视图复用原资产卡不新开第二张）；分镜重新提交改按镜号复用旧卡（D4 方案 A 落地，多余旧卡保留防断链）；关键帧阶段新增「打回重出」（`reject_keyframes` → 回 `executing` + 带意见唤醒，重出后再弹确认条）；注册表写前重读 + 内存合并 + 删除墓碑（CV-046 取方案 ②，不迁目录）。详见 [STATUS.md](./STATUS.md) §2.1 |
| 2026-09-17 | **CV-201 打包链路实测验证 + 两项结论修正（代码已落地，DMG 出包待普通终端复跑）** | 用户拍板「mac 维持 universal 单包 / 先按现状 GPL 落地」后，做真实打包验证，**两项结论被证据修正**：**① 包体**：`electron-builder --mac dir --universal` 实跑后量得未压缩合计 **118 MiB**（darwin-arm64 43 MiB + darwin-x64 75 MiB），原文档「+42MB」只是下限估算 —— §7 B1 与 plan §4 已按实测校正，DMG 压缩后真实增量仍待全量构建。**② 许可（重要）**：拉出产物二进制的 configure 行＝`--enable-gpl --enable-version3 --enable-nonfree --enable-libx264 --enable-libx265 …`。`--enable-gpl --enable-version3` ⇒ 二进制确为 GPL-3.0-or-later（文档没写错），**但 `--enable-nonfree` 触发 FFmpeg 随包 LICENSE「Incompatible libraries」节的明文**：*"…pass `--enable-nonfree` to configure. **This will cause the resulting binary to be unredistributable.**"* ⇒ 现状**不是「履行 GPL 义务即可分发」，而是上游禁止再分发**。拍板 ② 的前提因此被推翻：**内测/自用不受限，对外发布前必须换 LGPL 构建**（§7 B2 已从「复核项」升级为「阻断项」）。**③ 打包链路本身验证通过**：`afterPack` 闸在两个架构临时 app 上各过一次 —— x64 打 `no executability smoke for this host; verified darwin-x64 structurally`（非原生架构按设计转结构校验），arm64 打 `darwin-arm64 reported "ffmpeg version 6.0" in 7380ms`（**打包环境内真执行**，远超 5ms 反空绿闸）；产物 `Contents/Resources/ffmpeg/{darwin-arm64,darwin-x64}/` 两份二进制 **sha256 与 pin 值逐字节相符**、体积 45,568,216 / 78,862,176 相符、`LICENSE` 齐备；两架构临时 app 内容**完全相同** ⇒ 按架构分路径的设计确实绕开了 universal 合并冲突。**④ 未能跑完的环节与原因**：universal 合并 + DMG 出包被**本机 agent 文件写代理**拦下（`@electron/universal` 第一步就 `cp -R` 整个 `.app`，其中 `Contents/Resources/app.asar` 带 **`com.apple.provenance`** ⇒ 代理拒绝搬运，报 `Brokered file token refused: modify backup failed`，`electron-builder` 只显示为 `Operation not permitted`）。**判据是文件属性而非路径**：该文件 cp 到工作区内同样被拒，而往同一临时目录新建普通文件正常；带 `dangerouslyDisableSandbox` 复跑仍在同一处失败 ⇒ **不是产品缺陷，也无法在受管 shell 内绕过**，**需在普通终端复跑 `dist:mac-smoke` 收尾**。**⑤ 顺带发现两个待办**：(a) `dist:mac-smoke` / `dist:mac` 自身**不设 `ELECTRON_MIRROR` / `ELECTRON_BUILDER_BINARIES_MIRROR`**（只有 `start-canvas-studio.sh` 设了），无 VPN 环境下会挂在 GitHub 下载上（实测卡满 600s 单请求超时、整场 10m12s 全耗在此）；(b) `scripts/verify-mac-smoke.ts` 校验了 Info.plist / 主可执行文件 / app.asar / 7 个原生依赖，**但未断言随包 ffmpeg** —— 目前 ffmpeg 的守卫只在 `afterPack`（已实测会真跑真执行），缺一道 DMG 层的独立断言 | 详见 [plans/bundled-ffmpeg.md](./plans/bundled-ffmpeg.md) §7/§8；本轮无代码改动（仅文档校正），代码状态仍为**待验收** |
| 2026-09-20 | **CV-215 后端单任务纪律下沉 + 上传超时补齐（代码 + skill + 守卫，待验收）** | 用户三问（① 调后端超时怎么设 ② 失败后怎么探活 ③ 后端单任务规则有没有让模型知道）核查出的三项落地。**① 纪律从未进模型上下文** —— `skills/` 与 24 个工具描述**零处**提及，唯一写「调用一律串行」的是 `docs/api.md`（开发者文档），而总纲第 7 步**反向**写着 upload_image「（可并行）」。新增 `config.ts#DRAMA_SERIAL_HINT`（**唯一源**，仿 `COUNT_MODE_HINT` 防漂移）挂到 4 个批量风险工具（`image_generate` / `upload_image` / `video_generate` / `video_composite`）描述 —— 工具描述每回合都在上下文，比 SKILL.md 更可靠；总纲第 7 步改「**逐个**…（后端同步单任务）」；完整纪律（受约束 11 个打后端工具 / 不受约束 2 个本地 ffmpeg 工具 / 为什么并发只排队）写进分册 `toolchain.md`。**② 上传是唯一裸 fetch**（CV-135 漏网）：既不注入 `longRequestDispatcher`（传输层仍 undici 默认 300s）也无超时 ⇒ 补 `UPLOAD_TIMEOUT_MS`（5min）+ dispatcher + 三档错误文案，并纳入既有不变量守卫（必须 < 900s）。**③ `maxParallel` 定性为刻意不接入**：三处俱在零消费者，设置页与 `host-config.ts` 双处写明「后端同步单任务 ⇒ 并行无收益」。顺带勘误 `video_composite` 两处「Drama 最多 6 张」（CV-191 改 9 的漏网）。**关键发现**：串行**已被平台默认兜住** —— 24 个工具均未声明 `isConcurrencySafe`，而 dsh-tools 是 fail-closed（只有精确 `true` 才 parallel）⇒ 同回合多 tool_call 本就串行 + 排序屏障；**但这是隐式保护**，谁加一句 `isConcurrencySafe: () => true` 就静默失效 | 守卫 2 条（`tests/skill-guardrail.test.mjs`：总纲不得再出现「可并行」+ 常量内容与 4 工具引用），**反向变异已验证精确变红**（改回旧措辞 → 断言 3 红；删 video_composite 引用 → 断言 4 红并点名）；build + typecheck + verify:loader ✓，`tests 799 / pass 791 / fail 8`（8 个为既有基线、与本批**零依赖**，最终计数未变）。⚠️ 总纲净增 22 字节 → **15343/15360，15KB 闸门只剩 17 字节**，下批内容需等量删。**缓做**：宿主侧单通道队列 + 队列位次 UI（原方案 3）与探针升级「忙/闲」双态（原方案 5）—— 用户正请后端加「是否有任务在执行」接口，等接口到位再评估 ⇒ **两项均已落地**：探针双态＝CV-219（2026-09-21）、宿主队列 = CV-220（2026-09-21） |
| 2026-09-20 | **CV-216 放手跑卡片判据错位修复 + 结算文案层级读错修复（代码 + skill + 守卫，待验收）** | 用户截图问「放手跑下为什么还弹选项」。**取证不靠猜**：解 `session.jsonl.zstd`（**多帧**，须 `/opt/miniconda3/bin/zstd -dc`，Node 只解首帧）逐帧读 seq 1085/1086 —— `tool/call` 与 `tool/result` **相隔 7ms**，结果原文「（放手跑模式）本回合不等待用户作答…已直接按默认值取…」⇒ **Host 干净**（`autoAnswerFor` 按设计短路：不写 `pendingQuestion`、不进轮询）。**根因是客户端判据错位**：`client/question-capture.tsx` 的 `match()` 只看 `tool/call` 的工具名就建节点、`start()` 直接照参数建卡片，**从不问 Host 有没有真的问** —— 两条路互不知情。**修法（方案 A：按调用粒度落一个可识别标记）**：`studio-defaults.ts` 新增 `AUTO_ANSWER_MARKER` / `AUTO_ANSWER_OPTION_PATTERN`（**唯一源**），`autoAnswerFor` 首句改用它拼；新建根级纯模块 `src/question-result.ts`（**判定与渲染分家** —— 消费方是 `.tsx`、进不了 `node --test`，同 CV-151 的 `style-grid.ts` 手法）；客户端读到标记 ⇒ 翻 `autoAnswered` ⇒ **不渲染任何可交互元素**，降级为一行窄条。**为什么不整条隐藏**：它是「本该提问、但放手跑替你定了」的唯一痕迹，与画布上其它节点一样应可追溯。**为什么不读 `workflow.mode`**：判定按**调用粒度**生效 —— 重放旧会话读到的是当时的真相，读「当前模式」会让历史卡片形态漂移。**顺带挖出两个同源缺陷**：② `extractResultNote` **层级读错** —— 真实形状 `message.content[0] = {type:'tool-result', content:[{type:'text'}]}`，旧实现只遍历外层 ⇒ **恒回退「已结算」⇒ 所有卡片结算文案一起是坏的**（含用户答完后的「用户的选择：X」）；③ `StudioQuestionChatData.answer` 是**死字段**（只有赋 `null` 一处、从无读取 ⇒「✓ 已选择：X」是死分支）。**skill 层（真因之二）**：`clarification.md` 的硬命令在前、放手跑豁免压在第 26 条与文末 ⇒ 模型推理原文通篇在绕「§3 形态题的条件式跳过规则」、**一次没提放手跑**；⇒ 短路提到**文件首节**，并把宽泛的「唯一例外」（风格 skill 内部卡片）收紧为**三条件同时成立**（该 skill 本回合真的加载过 / 卡片写在该 skill 自己的 STEP 里 / 用户显式点名选中） | 守卫 **11 条**（新 `tests/question-result.test.mjs`：解析层 4 + 标记识别 3 + 咬合接线 4），含一条**端到端**断言 `autoAnswerSummary(autoAnswerFor(…))` 必须非 null —— 生成侧或消费侧任改措辞即红。**反向变异 3 轮全部精确变红**（改措辞 → **仅端到端那条**红；改回硬编码字面量 → 常量脱钩那条红；删 `autoAnswered: true` 翻牌 → 消费侧那条红，**并暴露原断言只查「标识符存在」不查接线、已加强为「算 / 翻 / 画」逐条钉**）。build + typecheck + verify:loader ✓，`tests 810 / pass 802 / fail 8`（净增 11、失败清单与基线逐条一致），预览台 23/33（失败为基线）。**顺带实测（后端侧，非本批改动）**：`GET /api/v1/health` **稳定 500**（0.02s、5/5 次，`/` 同 500；`/view` 返 422 说明路由与服务是活的）⇒ 用户给的 `queue_task_count` 此刻取不到；**更麻烦的是 `ensureDramaReachable` 是 fail-closed** ⇒ health 500 会让**所有打后端的操作**立即报「不可达」（**可观测性接口故障被升级成服务不可用**）⇒ ③（宿主单通道队列）⑤（探针升「忙/闲」）继续挂起。验收用例见 `acceptance-test-cases.md` **M 组**（M-1~M-7） |
| 2026-09-20 | **CV-217 占位「文案」卡收口（代码 + 守卫 10 条，待验收）** | 用户截图问「画布上经常出现一个占位节点，是出现的时机不正确吗」。**先取证，不靠截图推断**：① 读真画布 `~/.videobuddy/…/projects/北京书市/canvas.json` —— 两张「文案」节点，`d193cb86` 正文 `占位`（createdAt 16:12:16）与 `0303622f` 真文案（16:29:24）；② 解会话转录（`/opt/miniconda3/bin/zstd -dc`，多帧）看 `tool-call-chunks`，args 逐 token 为 `["", "{\"script\": ", "\"占位", "\"", "}"]` ⇒ **不是流被截断，是模型自己写的**，而它同一 step 的推理写着「Next, write the screenplay」；③ 同模式在「测试分辨率拆分」项目**独立复现第二次**。⇒ 结论：**不是「时机不对」，是那张卡从来没人回填** —— `write_script` 每次调用都 append，模型「先占位、后回填」的预期落空。**为什么不能靠原地覆盖解决**：`docs/canvas-lineage-completion-plan.md` §2.3 记着本产品「6 文案 ↔ 6 BGM ↔ 6 成片」的**多版文案是合法用法**（`supersedes` 链只覆盖视频），改成原地更新会砸掉它 —— 那属 CV-207，仍待拍板。**落地三处**：① 新增 `src/text-guard.ts`（纯模块，`tsc` 进 `lib/` 供 `node --test` 直连；同 CV-151 `style-grid.ts` 手法）—— `stubTextReason` 拦写入、`isPlaceholderOnlyText` / `isStubTextNode` 管载入；② `write_script` / `write_screenplay` 落盘前拒收占位载荷，错误文案复用 `STUB_PAYLOAD_RULE`（唯一源）并给出路「内容没准备好就先不要调用本工具」；③ 客户端 `setNodes` 载入清洗增 `isStubTextNode`，清历史遗留垃圾（只认 agent 写的文案/剧本节点，用户手动建的不动）。**判据口径**：**整篇级**而非子串匹配（真实文案里出现「不要写占位」这类叮嘱是合法的），且**载入侧比写入侧窄一档**（空内容不算占位）—— 写入拒收最坏是多跑一轮，载入误判是**静默数据丢失**。**验证**：build + typecheck + verify:loader ✓；`tests 820 / pass 812 / fail 8`（净增 10，失败清单与基线逐条一致）；预览台 23/33（基线）；**反向变异 2 轮共 4 处全部精确命中**（改 description 字面量 → 仅断言 8 红并点名 `write_script`；摘载入过滤 → 仅断言 10 红；拆 `origin` 校验 → 仅断言 4 红；把守卫条件写残 → 仅断言 5 红），其余断言全绿。**顺带勘误一处**：`tests/generate.test.mjs` 的原夹具传的是「广告词…」三字占位串，正好落在新守卫枪口上（该用例测血缘挂接，不是载荷校验），已改为合法文案。 |
| 2026-09-20 | **CV-218 落地：image2fix 修复 prompt 形态改造（代码 + 守卫 11 条，待验收；B 真机 A/B 被后端 upload 500 阻塞）** | 用户给出后台同事的**真实成功案例**，反证 CV-212 自动模板的 prompt 形态错误。**正例取证**：`api-probe/image2fix-20260920-text-spec/`（两张对照图 + report.md）—— 海报「山见茶事」12 处文字中 **8 处错**（春山一奁／新茶初醴／明前头索·高山云莠／鲜时自日袂桂／零投加蔗糖／冷睪 8 小盹／开盟限定／青石芢 27 号），修复 prompt 按「**文字规格段 + 逐字约束段**」写法喂入 ⇒ **8 处全修对、排版零漂移、无新增文字**。**规则**：修复 prompt = 原 prompt **减去**「画幅／材质／光线／配色／气质」等美术描述段。**反证**：CV-212 模板只喂字符列表 ⇒ 实测 `武仔`→`武传`（**仍错**）+ 凭空多出两处「武侠」+ 曝光漂移。**三条根因**：① **丢位置锚点** —— 模型不知该位置原本是什么字，只能按形近字猜；② **祈使式动词**「确保…正确渲染」⇒ 语义被读成「把这些字渲染出来」而非「把画错的字改对」；③ **无「不得增删」约束** ⇒ 顺手在别处补字。**讽刺点**：`image_fix` **工具描述早已写对**（「从原出图 prompt 里只提取文字那部分描述」），只有**自动路径** `runTextAutoFix()` 没跟上 —— 它只收 `quotedTexts`，把原 prompt（`a.prompt`，同一作用域内可得）丢掉了。**候选方案 A**：`runTextAutoFix` 增传原 prompt，新建纯函数 `extractTextSpec()`（按中文句号／分号／换行分句，保留**含引号文本的句子** + 含「逐字／准确还原／不得替换／不得增删」的**约束句**），抽不出时回退现有模板；**B**：对同一原图做老／新模板 **A/B 真跑对照**（各约 130s，后端单任务须串行）；**C**：**收紧误触发**（丢「引号前 3 字含 不要／不用／避免／不是／禁用／无」的句子，防「不要『水墨』风格」误命中）。**阻塞**：C 是否必须，取决于后端答复「**image2fix 对本来没有文字的图会不会凭空加字**」—— 已登记 `canvas-studio-api-usage.md` §5 第 6 条。**2026-09-20 已落地（A + C；B 待后端）**：① 新建纯函数 `extractTextSpec()`（分句 → 保留含**未被否定**引号文本的句子 + 含逐字约束关键词的句子 + 含「占位」的占位元素句；否定判据取**引号前 3 字窗口**，只认 不要／不用／避免／不是／禁用／别用／勿用／禁止 —— **刻意不含「无」「非」**，否则 `用无衬线"SALE"` 会被误判为否定）；② `buildTextFixPrompt(originalPrompt)` 改**原 prompt 直通**，并**删掉字符清单回退分支**（实际不可达：触发修复 ⇒ 原文必有含引号的规格句，留着等于第二份永不执行的实现）；③ `runTextAutoFix()` 增收原 prompt、`image_generate` 调用点传 `a.prompt`；④ 原文无约束句时补 `DEFAULT_TEXT_CONSTRAINT`。**离线校验即命中正例**：拿正例原 prompt 跑新逻辑，产出的修复 prompt 与人工确认的目标 prompt **逐行完全一致（0 处差异）**，夹具固化为 `tests/fixtures/text-spec/`。**另发现一条比预期更好的结论**：占位元素句（「右下角一个正方形细线空白方框，作为二维码占位」）不含引号也不含约束词，**必须单独保留**否则会被重绘掉 —— 离线校验第一轮就抓到了它（差 1 行）。**验证**：build + typecheck + verify:loader ✓；`tests 831 / pass 823 / fail 8`（净增 11；**stash 基线对照**：改前 `812 / 8`、失败清单**同名同编号**）；预览台 23/33（基线）；**反向变异 4 轮全部精确命中**（摘占位句规则 → 红 3；调用点漏传 `a.prompt` → 红 1；加回旧祈使句 → 红 2；摘否定过滤 → 红 3）。**skill 同步 5 处**（prompt-writing 模板段重写 / consistency 检测点 / krea2-turbo 与 krea2-edit 的「文字出错了怎么办」/ music-video-subtitle 的 Typography recovery）+ `image_fix` 工具与 prompt 参数描述。⚠️ **B 未跑成**：`/api/v1/health` 200、`openapi.json` 200，但 `POST /api/v1/generate/upload` **稳定 500**（3/3 次、~50ms 快失败，125KB 小图同样 500 ⇒ 非体积问题）；image2fix 依赖上传句柄（CV-155）⇒ 探针 `scripts/probe-image2fix-ab.mjs` 已就绪（同一原图 + 同一句柄，A 旧模板 / B 新逻辑各跑一次 + health 夹心 + 产物落盘），后端修好一条命令补跑。**2026-09-21 B 已跑完，判据被数据推翻并重立**：后端恢复后补跑两轮（探针新增 `--handle` 复用句柄 / `--tag` 多轮隔离）。**两版都能修对主字**（`武仔` → 都成 `武侠`）⇒ 上一轮「旧模板修成 `武传` 仍错」**不可复现，不能作判据**；**稳定的差别是凭空增字：旧模板 2/2 轮命中**（第 1 轮远景无中生出一面虚焦「武侠」红幡，第 2 轮把摊前不可读伪字整块换成「武侠」），**新逻辑 0/1 轮命中**。机理＝旧模板开场是**祈使式**（把字符说成「要渲染出来的东西」），新逻辑给的是**位置规格**。**曝光一项纠正**：灰度均值 原图 79.57 / A 76.61 / B 83.15 / A-r2 91.14 ⇒ 「新逻辑曝光更稳」**不成立**，整图重绘本身有 ±4 抖动。**样本量**：新逻辑仅 1 次成功（第 2 轮 B 被他人 5 个并发提交挤到 420s 超时）⇒ **待补跑到 2~3 样本**，命令在 `api-probe/image2fix-20260921-ab/analysis.md` §四。 |
| 2026-09-21 | **CV-219 落地：health 探针 fail-closed 误判修正 + 「忙/闲」双态（代码 + 守卫 8 条，待验收）** | 承接 CV-215/216 挂起的「原方案 5」。**先测后改**：打桩 health 返 200 / 500 / 422 / 连接失败四种，改前 `ensureDramaReachable` 对**后三种抛同一句**「Drama Backend 不可达，请检查服务是否已启动后再试」—— 服务活着、只有健康接口坏掉时，**所有**打后端的操作一起被拦（可观测性接口故障被升级成服务不可用；2026-09-20 实测正是这个形状：`/api/v1/health` 稳定 500 而 `/view` 返 422）。**分层重写**：收到**任何** HTTP 响应 ⇒ 服务活着；**只在拿不到响应（连接失败 / 超时）** 时才抛。**「忙/闲」双态**：返回值由 `Promise<void>` 改为 `DramaQueueDepth = number \| null`；`queueDepthOf()` 只认非负有限数（字符串 / 缺失 / `NaN` / 负数一律 `null`，**不降级成 0** —— 把「未知」当「空闲」是这里最容易犯的错）；可达即缓存 30s（**含「活着但健康未知」**，不再为一个已知坏掉的接口反复付费），拿不到响应不缓存（瞬时抖动不被误判为长期不可达）。**两个消费点，都不新开通道**：① `generateAsset` 提交前探一次，`depth > 0` 时往既有 warnings 通道推排队位次（探针自带缓存 ⇒ 零额外请求）；② 本地超时的文案改成**清缓存重探**后的实时深度（「后端当前有 N 个任务在执行」/「后端当前空闲」/「队列深度未知」），替掉含糊的「后端可能繁忙」。**`queue_task_count` 语义本次实测钉死＝含正在执行的那个**（空闲 0 / 独跑 1 / 再排一个 2 / 被占满 5）⇒ 判「有活在跑」用 `> 0`，判「空闲」用 `=== 0`，不要写 `<= 1`。**验证**：clean + tsdown + tsc×2 + verify:loader ✓；`tests 836 / pass 828 / fail 8`（净增 5；8 条红**与基线逐条同名**，仅因 health 组插入而编号整体 +5）；预览台 23/33（基线）；**反向变异 3 轮精确命中**（忙态门槛写 `> 1` → 仅忙态红；判据改回 fail-closed → 恰 2 条红；`queueDepthOf` 接受字符串 → 仅纯函数红）。**顺带修测试桩自身缺陷**：`stubFetch` 原本在构造时安装 ⇒ 数组字面量 `[stubFetch(), stubFetch()]` 会一次性造两个桩、先造的永远装不上、还原时又把后造的当 `original` ⇒ 改显式 `install()`。**未做**：③ 宿主侧单通道队列 + 队列位次 UI —— dsh 的 fail-closed 只兜住**模型同回合的 tool_call**，而 `routes.ts:519` 那条客户端并发路径绕过它 ⇒ 仍需宿主显式队列，另立项 CV-220 ⇒ **2026-09-21 已落地（见下一行）** | `src/generate.ts` / `tests/health.test.mjs` |
| 2026-09-21 | **CV-220 落地：宿主侧单通道生成队列 + 排队位次可见（代码 + 守卫 13 条，待验收）** | 承接 CV-219 的「未做 ③」。**先把「问题是什么」算清**：后端同步单任务（并发只排队、墙钟不变）早已知晓（CV-215 已写进工具描述），**真正的代价不是慢而是误判** —— 客户端占位截止 `PENDING_TIMEOUT_MS = 660_000`（`client/index.ts`）是**从占位落卡起算**，而视频档 `DRAMA_TIMEOUT_MS.video = 600_000`（`generate.ts`）⇒ **只剩 60s 余量**；只要与任何东西重叠（本端重试 / 重出 / 模型同回合多调用 / 后端被人占满 —— CV-218 B 轮实测 `queue_task_count = 5`），排队的那个占位「还没轮到」就被判「生成超时」。而宿主**从来没有队列**：`/canvas-studio/generate` 一收到就发。**两层设计**：① **拦截点选在 `callDrama`**（`generate.ts:826`）—— 它是**同步生成**的唯一网络入口（图 / 图内文字修复 / 角色四视图 / 视频(drama) / 音乐全走它；fal 是异步三段式、upload / 文本工具 / health 各自独立）⇒ 「**只拦同步 Drama**」是**结构性保证**，不需要 `if (provider === 'drama')` 这种字符串分支；新建 `src/generate-queue.ts`（Host **进程级队列实例**，`withGenerateSlot(label, signal, run)`，`finally` 释放）+ `src/queue-view.ts`（**纯类型 + 视图函数、不持实例** ⇒ 可安全进浏览器 bundle，同 CV-217 `text-guard.ts` 手法）；快照**只带事实**（`active` / `waiting`），`busy` / `state` 由 `queue-view.ts` **单源派生**（不双源）；`GenerationQueueState` 不变式＝**存在 ⇒ `waiting ≥ 1`** ⇒ `active` 非空、下游无死分支。② **位次可见 + 排队不计入截止**：新增**只读**路由 `GET /canvas-studio/generate-queue`（走 `requestAllowed`，**不是** `mutationAllowed`）→ `client/api.ts#fetchStudioGenerateQueue`（坏形状 → `null`，静默保持旧值）→ `project-store.generationQueue` → 客户端 **2s 轮询**（只在 `pendingTimers.size > 0 \|\| clientGenerations > 0` 时开，闲时停并清 store）→ `StudioFrame` 经 `CanvasSurface` 传给 `CanvasNode`，渲染一行窄条「**生成队列：<标签> · 等待 N 个**」；**拉到非 null 队列态就重挂全部 pending 计时器**（`armPendingTimer` 是唯一挂表实现）⇒ 排队被显式延期。**两条刻意不做**：**不接 `maxParallel`**（后端同步单任务 ⇒ 并发无收益，设置页继续标注「不会生效」，恒为 1）；**不做「第 N 位」**（客户端只看得见自己的请求，缺 `requestId → 队列条目` 归属通道 ⇒ 标到节点上是假精度，已写成 `queue-view.ts` 头部的有意省略）。**验证**：clean + tsdown + tsc×2 + verify:loader ✓；`tests 849 / pass 841 / fail 8`（净增 13；8 条红与基线**同名同编号**）；反向变异 **8 轮**全部精确命中（无互斥 → 4 红 / abort 不出队 → 1 红 / 出错不释放 → 4 红 / 接到 upload → 1 红 / 唯一入口挪走 → 1 红并点名 / 客户端不延期 → 1 红 / 路由误用 `mutationAllowed` → 1 红 / label 漏项 + normalize 降级 → 2 红 / fal 反向引用 → 1 红）。**踩坑**：③「出错不释放」原本用 `catch` 释放 ⇒ 后续等待者**永不 resolve、用例挂死**（CI 只看到超时、看不到哪条断言坏了）⇒ 改 `finally`，并给所有「队列该前进」的 await 加 `beforeStall()` 800ms 竞速闸，把挂死翻成可读红。验收用例见 `acceptance-test-cases.md` **Q 组**（Q-1~Q-6） | `src/generate-queue.ts`、`src/queue-view.ts`、`src/generate.ts`、`src/routes.ts`、`src/client/{api,index,project-store,StudioFrame,canvas/CanvasSurface,canvas/CanvasNode}.ts(x)`、`tests/generate-queue.test.mjs` |
| 2026-09-21 | **血缘补全补账 + 补测（CV-204/205/206 正式条目 + 新立 CV-221/222，待验收/待拍板）** | 三条血缘改动此前已在代码里但零测试零登记（STATUS 记「已分配·未登记」）。**本批**：① CV-204 的成片血缘合并（clipIds + bgmNodeId + scriptId）在 Host 工具与客户端时间轴导出两路径各有一份内联 `new Set` 展开，收口为共享纯函数 `composedSourceIds`（`compose-selection.ts`，已在 client include 内）；② 新增 `tests/lineage-completion.test.mjs` 6 条（纯函数 + 源码接线守卫防内联复活 + CV-205 端到端三态 + CV-206 并集过滤幽灵与兜底两态）；③ 以罗大佑风格测试（2026-09-21，69 节点）逐节点取证：成片 v1 血缘 5 条 ✓ / 18 分镜卡全挂剧本 ✓ / 后 2 条 BGM 挂文案 ✓。**同批立项两个新缺口**：CV-221 BGM 早于文案落盘时兜底扑空（建议兜底链放宽 文案→剧本→分镜卡）、CV-222 分镜 11 重跑未传 `replaces` ⇒ 同镜位两条活动视频并列（会重复拼进成片；建议工具侧自动 supersede，与 CV-207 联动拍板）。设计源头见 `canvas-lineage-completion-plan.md` | `src/compose-selection.ts`、`src/host-tools.ts`、`src/client/StudioFrame.tsx`、`tests/lineage-completion.test.mjs` |
| 2026-09-22 | **CV-223 验收后修复：镜位区下移 + 镜 1 对齐首行 + 横向间距放宽（守卫 13→14 条，待再验收）** | 桌面验收「罗大佑风格测试」（83 节点 18 镜）通过，但发现**分镜区整体下移**、与头部之间空出一大片。**读码定位（非猜测）**：`headRows = Math.max(源素材数, 2)`（`canvas-view.ts:453`）是「头部行数」，而镜位行从 `headRows + 镜号` 起算（`:471`）⇒ **镜位区单方面为头部让位**；尾区（文案/BGM/成片）与头部（`LANE_SOURCE`/`LANE_SCRIPT`）都从第 0 行起排，三者在 X 上本就不重叠 ⇒ **不对称只发生在镜位区一处**（demo 源素材少 + `headRows` 下限 3，故未暴露）。真实画布 6 个源素材 ⇒ 让出 5 行 ⇒ 镜 1 落 y=2018。**修法**：`shotRow.set(shot, shot)`（行号 = 镜号）；注释写明共享行区间为何安全（泳道保证 X 分离，同尾区先例）与代价（行高取该行最高单元 ⇒ 头部行距随镜位行变高）。**实测对比**（同画布改前/改后各真跑一次）：镜 1 `2018 → 368`、包围盒 `9328 → 7678`（−17.7%）、**83/83 落位 / 顶层重叠 0 / 18 卡 18 行 / 18 托盘全与对应分镜卡同行 全部保持**。**测试**：+1 守卫「源素材数量不推高镜位区起点」（反向变异精确命中：改回旧式仅该条红）；「全局锚不进镜位行」判据由**行号更小**改为**泳道 X 分离**。全量 `867/862/5`（净增 1，基线同名）。**续办（同日 · 同一验收反馈）**：① 镜 1 再上移一行与创意**同行**（`Math.max(0, shot - 1)`，镜 1 y `368 → 40`；`Math.max` 兜住 0 / 负镜号免出负行）；② `ARRANGE_GAP_X` 48 → **80**（只放宽横向、纵向仍走 `ARRANGE_GAP_Y = 48`）⇒ 包围盒 `4596×7678 → 4820×7350`（宽 +4.9%、高 −4.3%），主泳道间隙 48 → 80，结构指标全保持。测试 1 的「头部在镜位之上」改写为「同行 + 泳道 X 分离」（反向变异精确命中）；**预览台同题对照**：`arrange-preview` 的 7 条失败在 `git stash` 收起改动后**同样存在** ⇒ 既有基线，合计仍 23/33 | `src/canvas-view.ts`（`shotRow` 去掉 headRows 偏移 + 镜 1 对齐首行；`ARRANGE_GAP_X` 48→80）/ `tests/canvas-arrange.test.mjs`（13→14 条） |
| 2026-09-21 | **CV-221/222 拍板并当日落地（待验收）** | **CV-221 → 方案 A**（用户以「BGM 两大类用法」视角拍板：① 配乐类剧本/文案先行，② 音乐先行类如罗大佑先定 BGM 再做 MV）—— 兜底链放宽为 文案→剧本→分镜卡（取场上最新 max createdAt），**时序自限性天然区分两类**：配乐类兜底挂上、音乐先行类保持空血缘（「源 BGM」的正确形态，罗大佑首条 BGM 重新定性为非缺陷）；方案 B（skill 纪律锁顺序）否决 —— 会禁止模式 ② 合法流程。**CV-222 → 方案 B** —— `planSupersede` 新增镜位级取代通道：锚点 = 新视频血缘分镜卡（`anchorShotCardIds` 含 CV-031 继承锚点）∪ shotNodeIds 现场验证；同卡活动视频自动 supersede，与显式 `replaces` 取并集。**语义反转**：「同卡不同参考图 = 不同子镜可并存」旧决策推翻（罗大佑分镜 11 重复拼镜风险正是其代价），并行变体改走「旧版恢复 + 显式 clipIds」；工具描述同步 2 处；存量不迁移。测试 +6 反转 1，全量 860/852/8（基线同名） | `src/shot-versions.ts`、`src/generate.ts`、`src/host-tools.ts`、`tests/shot-versions.test.mjs`、`tests/lineage-completion.test.mjs` |
