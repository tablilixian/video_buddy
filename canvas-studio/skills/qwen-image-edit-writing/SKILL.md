---
name: qwen-image-edit-writing
description: 图生图与图像改写的指令式提示词规范（canvas-studio 的 image_generate 传参考图路径 + character_generate 三视图，Qwen-Image-Edit 技术路线）。凡基于已有图片生成新图前加载：操作+目标+规格+保留子句四段式、多参考图分工、局部改写的分步链式策略、文字编辑引号锁定规则，以及 inpaint / style_transfer 禁用时的替代路径。
---

# 图生图与图像改写提示词规范

适用：`image_generate` **传了参考图**（`filename` 单图 / `filenames` 最多 3 张）的图生图路径，以及 `character_generate` 角色三视图。**纯文生图不要用本规范**，改加载 `z-image-prompt-writing`。

## 核心区别：指令式，不是描述式

图生图端点接受 1–3 张参考图（后端 `image2image`，Qwen-Image-Edit 技术路线）。提示词描述的是**对输入图做什么**，而不是「成图长什么样」。

- ❌ 描述式（`A red sports car on a mountain road at sunset`）→ 模型无视输入图、凭空重画，角色/构图全丢。
- ✅ 指令式（`把车的颜色改成红色，其余元素完全一致`）→ 保留构图、打光与未提及的部分。

**一句话判断**：要「在原图上改一点」用指令式；要「借参考图的角色/风格重画一个新场景」用「保留子句 + 新场景描述」的混合式（见下）。

## 四段式公式（每次必写全）

```
操作（动词）+ 目标（具体部位/对象）+ 规格（改成什么样）+ 保留子句（什么不许动）
```

| 段 | 写法要点 | 例子 |
|---|---|---|
| 操作 | 替换 / 移除 / 添加 / 调整，一次只做一个 | 替换 |
| 目标 | 具体部位或对象，同类多物用位置消歧 | 前景那只狗 / 左边第二个人 |
| 规格 | 改成什么，含材质与光照方向 | 金毛犬 |
| 保留子句 | **最关键**，没有它模型会过度改动 | 保持打光与背景完全一致 |

完整例：`把前景的狗替换成金毛犬，保持打光与背景完全一致，其余元素全部保持不变。`

**保留子句不是可选项。** 每句提示词末尾都要有，哪怕只是「其他部分尽量不变」。

## 分步链式（复杂改动必用）

一次请求只做一个操作。「改发型 + 换背景 + 加墨镜」写在一起会互相干扰。复杂需求拆成链式多步：上一步产物 → `upload_image` → 作为下一步参考图，**每一步都重申保留约束**。

顺序原则：先大后小（背景/构图 → 主体 → 细节），每步都写保留子句。

## 多参考图分工（filenames 最多 3 张）

传多张时提示词里说明每张图的用途，模型才知道谁提供身份、谁提供风格：

```
参考图 1 提供角色身份与面部特征，参考图 2 提供场景与打光氛围，
合成一张自然合影，两人身份特征各自保持不变，背景沿用参考图 2
```

单张参考图走 `filename`（等价 `image1`），多张走 `filenames`。

## 常见场景模板

| 场景 | 提示词骨架 |
|---|---|
| 换装/换发型（保身份） | 保持人物身份与面部特征不变，把上衣改成白色衬衫，发型改成短发，其他部分尽量不变 |
| 光线/氛围调整 | 把光线改成清晨侧逆光，增强轮廓光，保持产品外观、颜色与细节不变 |
| 视角/构图变化 | 保持产品外观、颜色与细节一致，把视角改为 45 度俯视，保持产品完整可见 |
| 材质替换 | 保持结构、形状与尺寸不变，把材质替换为雾面金属，保留刻线与阴影关系，光线方向不变 |
| 背景替换 | 保持主体角度、打光与反射不变，仅替换背景为抛光黑大理石台面 |
| 文字编辑 | 把标题文字「春季促销」改成「夏季大促」，保持字体风格、大小、颜色与位置不变 |

**文字编辑**：新旧文本都必须加引号，并显式锁定字体属性（风格/大小/颜色/位置）。中英文均支持，模型会保持原字体风格。

## 角色三视图（character_generate）

`character_generate` 走 `qwen_4view_char_2step` 工作流，**不接受 prompt 参数**（只吃设计图）。三视图质量完全取决于输入设计图质量 —— 想改三视图只能回头改设计图，不要用提示词尝试纠偏。

## 改图工具现状（重要：不要调用禁用工具）

| 工具 | 状态 | 替代路径 |
|---|---|---|
| `inpaint`（局部重绘） | **暂不可用**，调用直接报错 | 用 `image_generate` 传参考图 + 保留子句实现局部改写 |
| `style_transfer`（风格迁移） | **暂不可用**，调用直接报错 | 风格统一用图生图传风格参考图，或 `character_generate` |

遇到「只改画面某一小块」的需求：**不要调 `inpaint`**，走图生图 + 四段式（目标写清部位 + 保留子句写死其余不变）。用户坚持要精确局部重绘时，如实说明当前不具备该能力。

## 负向提示词

- 文生图路径（无参考图，Z-Image）：**禁止使用** `negativePrompt`，模型忽略它，约束改写成正向表述。
- 图生图路径：若后端走 Qwen 分支，负向提示词有效，可谨慎使用（如 `low quality, noise, extra elements`）；但**优先把约束写进保留子句**，更可控。

## 输入质量

参考图分辨率越高细节保留越好（建议 ≥512×512）。参考图来源是画布产物时，先 `upload_image` 拿 filename，或直接在对话里用 `@ref[显示名]` 引用画布上的图（参考托盘节点与对话附件素材均可——附件已自动标记为参考）。

## 自检清单

- [ ] 写的是「操作」还是「成图描述」？（后者要改写成指令式）
- [ ] 保留子句写了吗？
- [ ] 一次只做了一个操作吗？（多于一个 → 拆步）
- [ ] 多参考图时说明每张的用途了吗？
- [ ] 改文字时原文 + 新文都加引号了吗？
- [ ] 有没有误调 `inpaint` / `style_transfer`？
