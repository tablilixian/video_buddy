# Canvas Studio 全功能验收测试用例集

> 版本：2026-08-29（对应批次 0 + 1 + 2 首批全部落地后的代码状态，测试 116/116）
> 用途：桌面端手动回归的完整用例清单。每条用例给出**操作步骤**与**正常表现**（预期结果）；
> 「正常表现」之外的任何稳定复现现象都算缺陷，回填到 §十一 缺陷记录表。
> 用例编号规则：`模块-序号`，标注关联 backlog 条目（CV-xxx / HITL-x）方便追溯。

---

## 〇、验收前准备

1. 构建 + 重启桌面（**每次代码变更后必须重建**，桌面只加载 `lib/` 编译产物）：
   ```bash
   corepack yarn workspace canvas-studio build && corepack yarn dev
   ```
2. 打开一个已有内容的项目（含生成节点、参考图、血缘边的项目最理想）；没有就先跑一遍 §八 M 组造数据。
3. 记录桌面版本与分支提交号，缺陷记录需附带。

判定总原则：
- **通过** = 表现与用例「正常表现」列一致，且不引入新异常。
- **有条件通过** = 功能生效但有轻微瑕疵（如动画缺失），记录不阻塞。
- **不通过** = 功能不生效 / 报错 / 产生错误数据。

---

## 一、A 组：项目管理

| 编号 | 前置 | 步骤 | 正常表现 |
| --- | --- | --- | --- |
| A-1 | 无 | 新建项目 | 项目出现在列表并自动选中；画布为空但网格/工具栏正常渲染；无报错 toast |
| A-2 | 已有 2 个以上项目 | 点击列表另一项目 | 画布切换为该项目内容；当前会话跟随切换；左右栏不残留上一项目的选中态 |
| A-3 | 项目甲存在 | 删除项目甲 → 再新建同名项目 | 新建成功，**不报** `workspace rename failed: workspace-name-conflict`（CV-033） |
| A-4 | 有历史残留孤儿 workspace 的旧环境 | 重启桌面，观察画布映射 | 「对话有内容 ⇒ 画布跟随当前会话」「列表选中项与画布一致」，三者不互相矛盾（CV-034） |
| A-5 | 任一项目 | 重启桌面后打开 | 上次的项目与画布状态恢复；对话与画布对应关系正确 |

## 二、B 组：画布基础交互

| 编号 | 前置 | 步骤 | 正常表现 |
| --- | --- | --- | --- |
| B-1 | 画布有节点 | 空白处按住左键拖动 | 画布平移，节点跟随移动，松手不回弹 |
| B-2 | 画布有节点 | 滚轮缩放 | 以指针为中心缩放；缩放过程中节点文字/边线清晰 |
| B-3 | — | 观察背景网格 | 网格线明显**浅淡**（45% 透明度、跟随明暗主题），格距 40px；不产生视觉噪音（CV-035） |
| B-4 | 节点分布在视野外 | 点击 Minimap 上某位置 | 视口居中跳转到该位置，跳转中心准确落在**画布中列**（不因左右栏宽度系统性偏移）（CV-003）；Minimap 视口框与实际视野一致 |
| B-5 | 画布有内容 | 双击画布空白处 | 视野适配全部内容（fitToContent）（CV-019） |
| B-6 | 选中若干节点（可 Ctrl 点选多个） | 触发工具栏/快捷的「缩放到选中」 | 视野缩放到恰好框住选中节点；无选中时退化为 fitToContent（CV-019） |
| B-7 | — | 切换明暗主题 | 网格、边线、节点、菜单全部跟随主题，无白底黑字错乱 |

## 三、C 组：节点创建与文本编辑

| 编号 | 前置 | 步骤 | 正常表现 |
| --- | --- | --- | --- |
| C-1 | — | 工具栏新建便签/文本/提示 | 节点落在网格空位（不叠在原点），可拖动 |
| C-2 | — | 右键画布空白 → 「在此新建便签/文本/提示」 | 节点**落在右键光标处**（节点左上角对齐光标）；菜单先关闭再创建（CV-016） |
| C-3 | 已复制过节点 | 右键空白 → 「粘贴」 | 副本出现且不与原件完全重叠；无剪贴板时「粘贴」可点但无效果或置灰（以实测为准，记录表现） |
| C-4 | 存在文本类节点（sticky/text/prompt） | 双击该节点 | 进入**节点内联编辑**（textarea），失焦/Enter 提交、Shift+Enter 换行、Escape 取消（CV-001，D1 方案 A） |
| C-5 | 文本类节点 | 双击打开详情面板 → 编辑「正文」区 → 失焦 | 正文写回节点并持久化；切到其它节点再回来，草稿不串位（CV-001） |
| C-6 | 媒体节点 | 双击 | **图片**双击弹出大图预览浮层（CV-045，见 C-10）；**视频**双击打开固定尺寸播放浮层（CV-044，见 C-9），不再触发原生桌面全屏 |
| C-7 | 详情面板处于某节点打开态 | **单击**另一个节点 | 详情面板**不跟随**切换（单击 ≠ 双击语义，CV-030）；再双击该节点才打开 |
| C-8 | — | 右键节点 → 重命名 → 输入新名 | 标题即时更新且持久化；重启后保持 |
| C-9 | 有视频节点（生成或上传均可） | 双击视频节点（CV-044） | 弹出**固定尺寸播放浮层**：视频按真实分辨率渲染、宽上限 960px / 高上限 80% 视口、保持原始宽高比（横竖屏都合适，不铺满桌面）；浮层带标题栏与关闭 ×；视频自动开始播放；点浮层外黑色区域 / 按 Escape / 点 × 均关闭且播放停止；**双击视频画面不再进入桌面全屏**（原生全屏按钮已隐藏） |
| C-10 | 有图片节点（生成或上传均可） | 双击图片节点（CV-045） | 弹出**大图预览浮层**：图片按真实分辨率渲染、宽上限 960px / 高上限 80% 视口、保持比例（黑底衬托，不铺满桌面）；浮层带标题栏与关闭 ×；点浮层外黑色区域 / 按 Escape / 点 × 均关闭；详情查看改由右键菜单「查看详情」进入 |

## 四、D 组：节点右键菜单（CV-037 修复回归，逐项必点）

前置：右键任一节点，菜单弹出。**通用关闭行为**（每条用例都隐含验证）：
- 点菜单项 → 菜单先关闭、动作**真正执行**（修复点：此前 14 项全部点击无效）；
- 按 Escape → 关闭；点菜单外任意处 → 关闭；右键另一节点 → 旧菜单关闭新菜单打开。

| 编号 | 菜单项 | 步骤 | 正常表现 |
| --- | --- | --- | --- |
| D-1 | 重命名 | 点击 | 出现改名入口（详情面板/行内编辑），改名生效 |
| D-2 | 复制 | 点击 | 副本出现；随后右键空白「粘贴」能粘贴该副本 |
| D-2a | 查看详情 | 点击 | 打开该节点的详情 / 编辑面板（媒体类节点双击已改为预览，详情查看走此入口，CV-045） |
| D-3 | 引用到对话 | 点击 | 对话输入框光标处插入 `@ref` 引用标记；失败时回退为复制（不静默丢失） |
| D-4 | 下载资产 | 右键**带图片/视频 url** 的节点 → 点击 | 触发浏览器下载，文件名为清洗后的节点标题 + 正确扩展名（CV-020） |
| D-5 | 下载资产（置灰） | 右键无资产的节点（便签/文本） | 菜单中**不出现**该项（`canDownloadNode` 为 false 时直接不渲染） |
| D-6 | 锁定 / 解锁 | 点击锁定 | 节点不可拖动、方向键微调跳过它；再点解锁恢复 |
| D-7 | 隐藏 / 显示 | 点击隐藏 | 节点从画布消失（图层面板仍有记录）；再点显示恢复 |
| D-8 | 置顶 / 置底 | 与相邻节点重叠后点击 | 层级立即变化，视觉遮挡关系正确 |
| D-9 | 上移一层 / 下移一层 | 同上 | 每点一次只动一层 |
| D-10 | 解组 | 右键 group 节点 | 子节点散开为独立节点；非 group 节点**不出现**该项 |
| D-11 | 打断 | 右键生成中（isLoading）节点 | 生成任务取消，loading 遮罩消失；非 loading 节点**不出现**该项 |
| D-12 | 重试（同参数重新生成） | 右键 agent 生成且有提示词的非 loading 节点 | 节点回到生成中态并重新生成；**双击/快速连点只触发一次**（防重复生成守卫） |
| D-13 | 修改提示词 | 右键 agent 生成节点 | 打开提示词编辑入口（steer），提交后生效 |
| D-14 | 删除 | 点击（红色危险项） | 节点删除且相关边消失；undo 可恢复 |

## 五、E 组：选择与键盘

| 编号 | 前置 | 步骤 | 正常表现 |
| --- | --- | --- | --- |
| E-1 | — | 单击节点 | 选中高亮；单击空白取消选中 |
| E-2 | — | Ctrl/Cmd 点选多个节点 | 多选保持；拖动其中**被按下的那个**节点时其它选中节点是否跟随——**当前预期：不跟随（仅移动被按节点）**，这是已知半成品（CV-008 未实施），记录实测表现即可，不算新缺陷 |
| E-3 | 选中一个节点 | 按方向键 | 节点移动 1px（CV-017） |
| E-4 | 同上 | Shift + 方向键 | 移动 10px |
| E-5 | 选中节点 | 800ms 内连续快按方向键后按一次 Cmd/Ctrl+Z | **一次** undo 回到微调前（连按合并为一条 undo 快照）；每次按键仍即时落盘（刷新后位置保持） |
| E-6 | 选中**锁定**节点 | 按方向键 | 节点不动（锁定跳过） |
| E-7 | 菜单/浮层打开时 | 按方向键 | 不误触微调（键盘焦点语义正常，以实测为准） |

## 六、F 组：连线系统（CV-038 共享几何回归）

| 编号 | 前置 | 步骤 | 正常表现 |
| --- | --- | --- | --- |
| F-1 | 任意节点 | 从节点右缘 link handle 按下并拖动 | 起草线**从来源节点右缘中点出发**（不是指针按下点）；线是**贝塞尔曲线** |
| F-2 | 拖拽起草中 | 指针拖到来源节点**左侧** | 曲线镜像弯折、不打结（控制点镜像逻辑） |
| F-3 | 拖拽起草中 | 松手落到目标节点上 | 落定后的正式边与起草线**形态一致**（同一条 C 贝塞尔），视觉连续无跳变 |
| F-4 | 已有血缘边（agent 生成链） | 缩放到 0.3x 观察 | 线宽屏幕恒定约 3.5px（反向缩放补偿），箭头与 chip 同步补偿；低缩放下边仍可见（CV-032） |
| F-5 | 边较多 | 缩放到 < 0.6x | 边中点中文 chip 隐藏只留线；选中节点的相关边 chip 始终保留（CV-014） |
| F-6 | 创意节点 + 分镜/文案节点 | 观察自动血缘 | 创意 → 分镜 → 关键帧 → 视频的连线完整；关键帧同时连**所属分镜卡**（shotRefs / 血缘继承 CV-027/031） |
| F-7 | — | 手动连线只能加不能删 | 已知限制（CV-005 延后），不作为缺陷记录 |

## 七、G 组：详情面板

| 编号 | 前置 | 步骤 | 正常表现 |
| --- | --- | --- | --- |
| G-1 | 打开生成节点详情 | 观察参数区 | 提示词单独成段可复制；参考图缩略图正确反查显示；参数为一行摘要；原始 JSON 折叠可展开（CV-012） |
| G-2 | 生成节点 | 查看「修改提示词」输入框 | 预填当前 prompt，不用自己从 JSON 抠 |
| G-3 | 上传的图片节点 | 查看分辨率 | 显示真实宽高（落卡前已探测），不是「未知」（CV-013） |
| G-4 | 媒体节点详情 | 点底部「下载资产」 | 与右键下载行为一致（CV-020） |
| G-5 | 失败的生成节点 | 打开详情 | 可见错误信息；有重放参数时提供重试入口 |

## 八、H 组：Agent 工作流 / HITL（造数据用，也是回归用例）

| 编号 | 前置 | 步骤 | 正常表现 |
| --- | --- | --- | --- |
| H-1 | 新会话输入一条创意 | 发送首条真人消息 | 画布原点自动出现「创意」节点（toolName=user_brief）；每项目至多一个，重复发消息不重复落（CV-023）；旧项目打开时经历史重放自动补落 |
| H-2 | 有创意节点 | 让 agent 提交分镜表 | 分镜**逐镜拆卡**（「分镜 N · 景别」），每行 3 卡横向排列，血缘指向创意节点并排其右侧；解析失败回退整表单节点（CV-024/025/026） |
| H-3 | 分镜卡存在 | 让 agent 生成关键帧/视频，指定所属分镜 | 新节点连到分镜卡与素材来源，落位在来源右侧一列（重叠自动避让）；即使模型漏传 shotRefs，视频也继承分镜卡血缘（CV-027/031） |
| H-4 | HITL 逐步确认模式 | 分镜提交后 | 出现批准/驳回 UI；批准走分镜门禁；**已知现状：批准后 agent 不自动苏醒，需手打「继续」**（O1/HITL-B 未实施）——记录表现，不算新缺陷 |
| H-5 | agent 提问（allowFreeText） | 遇到开放性问题（如品牌名） | 点选卡片出现自由输入框 + 提交按钮；答案走同一通道；提交后锁定防重复提交（CV-002） |
| H-6 | 风格澄清第 ③ 步 | 两级追问 | 3a 显示四大类；选大类后 3b 显示该类下具体风格，options 为逐字预设名（保证路由命中）；skill 加载失败时重试一次后降级继续并说明，不卡死 |
| H-7 | 3b 选具体风格 | 观察 | 出现 GIF 预览卡片（2 列网格、懒加载、推荐项带徽标）；点选回流正常；**时长/画幅等非风格问题仍是文字按钮**（无 GIF 属预期，不算缺陷） |
| H-8 | 生成中节点 | 等待观察 | loading 遮罩显示已耗时 MM:SS 每秒跳动；超 3 分钟出现「可在详情面板或右键菜单打断」提示（CV-010） |
| H-9 | skill-catalog | 查看上下文注入面板 | 全部 9 个 MiniMax-H3 skill + canvas-studio-creation 总纲出现；**catalog 描述截断是正常 UI 行为**（详见 minimax-skills-acceptance.md） |

## 九、I 组：生成管线落卡（视觉/数据正确性）

| 编号 | 前置 | 步骤 | 正常表现 |
| --- | --- | --- | --- |
| I-1 | 生成 16:9 图片/视频 | 观察节点框 | 画布框为预览尺寸（16:9→480×270），不是媒体原始 1280×720（CV-028） |
| I-2 | 生成 9:16 / 1:1 媒体 | 同上 | 9:16→270×480、1:1→420×420；长边固定 480、短边按真实比例（CV-029） |
| I-3 | 上传非标比例素材 | 观察落卡 | 框按真实比例创建（落卡前探测）；媒体加载后偏差 >5% 自动校正（钳制 60–960）；锁定节点跳过校正；校正后不循环抖动 |
| I-4 | 生成完成 | 观察节点 | 媒体正常显示、无 object-fit 静默裁切（中间一条/裁边现象不应出现） |
| I-5 | retry 失败节点 | 触发重试成功 | 节点原地更新（retryOf），不产生新节点、位置不变 |

## 十、J 组：参考托盘 / 图层面板 / 时间轴 / 设置页 / Toast

| 编号 | 前置 | 步骤 | 正常表现 |
| --- | --- | --- | --- |
| J-1 | 无参考图 | 打开参考托盘 | 显示**空态引导卡**（虚线框，说明如何标记参考图及用途），不是直接不渲染（CV-011） |
| J-2 | 标记一张图为参考（角色） | 观察画布节点 | 节点左下出现「参考 · 角色」角标 + 红色角色色点（构图=蓝/角色=红/风格=紫/首末帧=青）（CV-011） |
| J-3 | 多节点项目 | 点击图层面板某行 | 画布**居中定位**到该节点（同 id 重复点击不重复跳转）（CV-009） |
| J-4 | 视频生成中 | 观察时间轴 | 视频片段 chip 计数正常；便签/文本等非媒体 chip 混排为已知现状（CV-007 延后，不算新缺陷） |
| J-5 | 打开设置页 | 查看「工作流与 HITL」分区 | 所有 reserved 前向字段（含「默认执行模式」）挂「待接入」角标，**没有伪装成已生效的开关**（HITL-C） |
| J-6 | 触发一次上传失败 / 成片成功 | 观察提示 | 底部居中出现**非阻塞 toast**（错误 6s / 普通 3.5s 自动消失），**不再弹系统 alert**（CV-015）；多条 toast 可堆叠 |
| J-7 | 成片合成成功 | 观察 | toast 提示成功，画布自动居中定位到成片节点（F1 行为）；成片节点可播放 |

## 十一、缺陷记录（验收后回填）

| 日期 | 用例编号 | 现象 | 复现步骤 | 严重度 | 备注 |
| --- | --- | --- | --- | --- | --- |
| | | | | | |

## 十二、已知限制（不是缺陷，勿重复提单）

| 项 | 关联 | 说明 |
| --- | --- | --- |
| 多选拖拽只动被按节点、group 不带 children、无框选 | CV-008 | 半成品已知，方案待拍板 |
| 批准后 agent 不自动继续 | O1/HITL-B | 需桥接对话发送能力，批次 3 候选 |
| 逐步确认只闸分镜一刀 | O4/HITL-A | 关键帧第二道闸待拍板 |
| 血缘连线不能删 | CV-005 | 已拍板延后（并入多版择优工作流） |
| compose 不能排除片段 / 时间轴语义混乱 | CV-006/007 | 已拍板延后 |
| 项目无「已完成」标记 | CV-036 | 方案 A/B 待拍板 |
| 重复提交分镜会追加新节点 | CV-026 已知限制 | 去重待做 |
| catalog 描述截断、3a 大类无 GIF 预览 | skill 体系 | 正常 UI 行为 |
| BGM/配音/字幕占位工具返回降级指引 | skill 体系 | 预期行为，流程应继续不卡死 |
